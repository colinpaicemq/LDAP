# Directory server configuration utility version 4.4
# Service level OA57794
# Build date Jul 29 2019, Time 13:01:24
# *********************************************************************
# This file is shipped in code page IBM-1047 and must remain in
# code page IBM-1047.
# *********************************************************************
#
# *********************************************************************
# Licensed Materials - Property of IBM
# 5650-ZOS
# Copyright IBM Corp. 2008, 2015
# *********************************************************************
#
# *********************************************************************
# This was generated by dsconfig on Thu Sep 23 04:18:18 2021
# This was generated by dsconfig with the input
# file: 'ds.profile'.
# WARNING: Any manual updates to this file will be lost
# if dsconfig is executed again and the OUTPUT_DATASET option is
# set to GLD.CNFOUT
# *********************************************************************
# This is the main configuration file for the IBM Tivoli Directory
# Server for z/OS LDAP Server. Refer to publication:
#
# IBM Tivoli Directory Server Administration and Use for z/OS
# (SC23-6788) for details on specifying each configuration option
# in this file.

# =====================================================================
# =====================================================================
# =====================================================================
# =====================================================================
#
# GLOBAL SECTION
#
# =====================================================================
# =====================================================================
# =====================================================================
# =====================================================================

#----------------------------------------------------------------------
# include <filename>
#
# Description:
#   The include option will cause the specified file to be included
#   as part of the LDAP server configuration.
#
# Example:
#   include //'sys1.global.parms'
#----------------------------------------------------------------------
#include "//'sys1.global.parms'"

#----------------------------------------------------------------------
# adminDN <distinguished name>
#
# Description:
#   The adminDN option specifies the distinguished name (DN) of the
#   administrator for this LDAP server.  Normally, this DN has
#   unrestricted access to all entries in the directory except
#   for entries in backends that are read-only replicas.  When the
#   LDAP server is in maintenance mode, the LDAP administrator has
#   unrestricted access to all entries in the directory.
#
#   Initially, the adminDN value should not contain a suffix.  This
#   allows the schema to be loaded.  When desired, it also allows the
#   adminDN to be loaded into the directory.
#
# Example:
#   adminDN "cn=LDAP Administrator"
#
# Recommendation:
#   After the schema is loaded it is recommended, though not necessary,
#   that the DN be loaded into the directory with the same suffix as
#   one of the suffix option values below.
#
#   Once the entry is loaded in the directory for this distinguished
#   name the adminDN should be changed to reflect the new DN.  The
#   entry is used when evaluating an LDAP bind operation for the
#   adminDN.
#
# Example:
#   adminDN "cn=Admin, o=Your Company"
#
# Notes:
#   This configuration option must be specified.
#----------------------------------------------------------------------
adminDN "cn=Admin, o=Your Company"

#----------------------------------------------------------------------
# adminPW <password>
#
# Description:
#   The adminPW option specifies the password for the administrator
#   defined by the adminDN option.
#
# Example:
#   adminPW secret
#
# Notes:
#   Use of the adminPW option is strongly discouraged in production
#   environments.  Instead, specify your adminDN as the distinguished
#   name of an existing entry in the directory information tree,
#   including a userPassword attribute.  This will eliminate
#   passwords from the configuration file.
#
#   This configuration option is optional.
#----------------------------------------------------------------------
adminPW secret

#----------------------------------------------------------------------
# allowAnonymousBinds <on | off>
#
# Default Value: on
#
# Description:
#   The allowAnonymousBinds option determines whether an LDAP client
#   can perform unauthenticated operations on the server. If
#   the option is set to 'off', clients must explicitly bind
#   to the server with a distinguished name.  If the option is set to
#   'on', a client may access the server without specifying a
#   distinguished name, and will have access to data as a member of the
#   'cn=anybody' group.
#
# Example:
#   allowAnonymousBinds on
#----------------------------------------------------------------------
#allowAnonymousBinds on

#----------------------------------------------------------------------
# altServer <ldap-url>
#
# Description:
#   The altServer option specifies the LDAP URL of an equivalent server
#   to this LDAP server.
#
# Example:
#   altServer ldap://alternate.server.com:3389
#
# Notes:
#   More than one altServer setting may appear in the configuration if
#   there are multiple alternate servers.  The URL must consist of
#   characters in the ISO8859-1 character-set.
#----------------------------------------------------------------------
#altServer ldap://alternate.server.com:3389

#----------------------------------------------------------------------
# armName <name>
#
# Default Value: GLDSRVR
#
# Description:
#   The armName option specifies the name that the LDAP server will
#   use when registering with the Automatic Restart Management
#   (ARM) service.
#
# Example:
#   armName LDAP1
#
# Notes:
#   The name is 1-7 characters.  The first character may not be a
#   number.
#----------------------------------------------------------------------
#armName GLDSRVR

#----------------------------------------------------------------------
# audit <on|off | all,operations | error,operations | none,operations>
#
# Default Value: off
#
# Description:
#   The audit option is used to turn LDAP auditing on or off.  It can
#   also specify which operations are to be audited and the associated
#   audit level.  When auditing is on, an LDAP SMF type 83 subtype 3
#   audit record is generated for an operation if the operation is
#   specified on an audit option and the operation result matches the
#   audit level.
#
# Examples:
#   audit on
#   audit all,add
#   audit error,modify+search+bind
#
# Notes:
#   This option can be specified multiple times, once to turn auditing
#   on or off and once or more times for each audit level to specify
#   the operations to audit for that level.
#
#   Supported values for 'operations' can be one or more of: add, bind,
#   compare, connect, delete, disconnect, exop, modify, modifydn,
#   search, unbind.
#----------------------------------------------------------------------
#audit off

#----------------------------------------------------------------------
# blockedConnectionTimeout <integer-timeout>
#
# Default Value: 0 (indefinitely)
#
# Description:
#   The blockedConnectionTimeout option specifies the amount of time in
#   seconds that the LDAP server will wait for a blocked send operation
#   to complete on a client connection.  When a send operation times
#   out, the client connection is dropped.
#
#   The value must be in the range from 0 to 2678400(equal to 31 days).
#   A value of 0 indicates that a send operation does not time out and
#   the client connection will remain active indefinitely.
#
# Example:
#   blockedConnectionTimeout 30
#----------------------------------------------------------------------
#blockedConnectionTimeout 0

#----------------------------------------------------------------------
# commThreads <integer-count>
#
# Default Value: 10
#
# Description:
#   The commThreads option specifies the number of threads in the
#   communication thread pool.  This thread pool handles connections
#   between the LDAP server and its clients.
#
# Example:
#   commThreads 20
#
# Notes:
#   At least one thread must be specified.
#----------------------------------------------------------------------
commThreads 10

#----------------------------------------------------------------------
# db2StartUpRetryInterval <integer-number>
#
# Default Value: 45
#
# Description:
#   The db2StartUpRetryInterval option specifies the amount of time
#   in seconds that the LDAP server will wait before each DB2
#   connection retry attempt.
#
# Example:
#   db2StartUpRetryInterval 60
#
# Notes:
#   The minimum value is 1, the maximum value is 999.
#   This option is only effective during LDAP server startup.  It is
#   ignored if the db2StartUpRetryLimit option has a value of 0.
#----------------------------------------------------------------------
#db2StartUpRetryInterval 45

#----------------------------------------------------------------------
# db2StartUpRetryLimit <integer-count>
#
# Default Value: 0
#
# Description:
#   The db2StartUpRetryLimit option specifies the number of times the
#   LDAP server will retry a DB2 connection before failing to
#   establish a connection with DB2.
#
# Example:
#   db2StartUpRetryLimit 5
#
# Notes:
#   The minimum value is 0, the maximum value is 99.
#   This option is only effective during LDAP server startup.  It is
#   ignored if no DB2-based backend (TDBM and DB2-based GDBM) is
#   defined.
#----------------------------------------------------------------------
#db2StartUpRetryLimit 0

#----------------------------------------------------------------------
# db2Terminate <terminate | recover>
#
# Default Value: recover
#
# Description:
#   The db2Terminate option specifies how the LDAP server will react
#   to a termination of DB2 after the server successfully starts.
#
# Example:
#   db2Terminate terminate
#
# Notes:
#   This option is ignored and no DB2 monitoring is done if no
#   DB2-based backend (TDBM and DB2-based GDBM) is configured.
#----------------------------------------------------------------------
#db2Terminate recover

#----------------------------------------------------------------------
# dnCacheSize <integer>
#
# Default Value: 1000
#
# Description:
#   The dnCacheSize option specifies the maximum number of entries to
#   store in the Distinguished Name normalization cache.  This cache
#   holds information related to the mapping of Distinguished Names
#   between their raw form and their canonical form.  Retrieval of
#   information from this cache reduces processing required to locate
#   entries in the database.
#
# Example:
#   dnCacheSize 500
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#dnCacheSize 1000

#----------------------------------------------------------------------
# idleConnectionTimeout <integer-timeout>
#
# Default Value: 0 (indefinitely)
#
# Recommended Value: 1800 (30 minutes)
#
# Description:
#   The idleConnectionTimeout option specifies the amount of time in
#   seconds that the LDAP server will wait on an idle connection or
#   idle paged search results.  When an idle connection times out, the
#   client connection is dropped.  When idle paged results time out,
#   the paged results set is abandoned.  Idle connections and idle
#   paged search results are detected by the LDAP server's network
#   monitor task, which checks for them every 30 seconds. Therefore, it
#   is possible for an idle connection or idle paged search results to
#   remain active slightly longer than the idleConnectionTimeout value.
#
#   The value must be either 0 or between 30 and 2147483647. A value of
#   0 indicates that an idle connection or idle paged search results
#   will remain active indefinitely.
#
# Example:
#   idleConnectionTimeout 60
#----------------------------------------------------------------------
#idleConnectionTimeout 1800

#----------------------------------------------------------------------
# healthcheck <checkName "checkParms">
#
# Description:
#   The healthcheck option defines a health check to the LDAP server.
#       checkName  - Specify the name of the health check.
#       checkParms - Optionally, specify check parameters, all
#                    checkParms must be within double quotes, and
#                    separated by blank characters.
#
#   LDAP server built-in health checks:
#       DB2 - Health check support for the TDBM and DB2-based GDBM
#             database maintenance. The only valid check parameter
#             'TOD HH:MM' specifies the time of day the LDAP server
#             runs the check. If not specified, the check will not
#             run automatically.
#       CONFIG - Health check support for he configuration settings.
#                No parameter accepted by this check.
#
# Example:
#   healthcheck DB2 "TOD 00:00"
#   healthcheck CONFIG
#----------------------------------------------------------------------
#healthcheck

#----------------------------------------------------------------------
# listen <ldap-url>
#
# Default Value: ldap://:389
#
# Description:
#   The listen option specifies, in LDAP URL format, the IP address or
#   host name and the port number where the LDAP server will listen for
#   incoming client requests.  This option may be specified more than
#   once in the configuration file to identify multiple interfaces.
#
#   To listen for non-secure requests on a TCP/IP network interface:
#     listen ldap://<ip-address | host-name | inaddr_any | in6addr_any>
#                   <:port-number>
#
#   To listen for secure requests on a TCP/IP network interface:
#     listen ldaps://<ip-address | host-name | inaddr_any | in6addr_any>
#                    <:port-number>
#
#   To listen for z/OS Security Authorization Facility (SAF) requests
#   for Policy Director or LDAP client requests using the Program Call
#   interface:
#      listen ldap://:pc
#
#   'inaddr_any' specifies the INADDR_ANY interface.  If specified,
#   the z/OS Communications Server determines the active and available
#   IPv4 TCP/IP interfaces on the system that the LDAP server binds
#   and listens for requests.
#
#   'in6addr_any' specifies the in6addr_any interface.  If specified,
#   the z/OS Communications Server determines the active and available
#   IPv4 and IPv6 TCP/IP interfaces on the system that the LDAP server
#   binds and listens for requests.
#
# Examples:
#   Listen for client requests from any hostname or IP address on
#   non-secure port 444:
#      listen ldap://:444
#
#   Listen for client requests from any hostname or IP address on
#   secure port 555:
#      listen ldaps://:555
#
#   Listen for PC calls on the z/OS SAF interface:
#      listen ldap://:pc
#
#   Listen for client requests from www.host.com on the default
#   non-secure port 389:
#      listen ldap://www.host.com
#
#   Listen for client requests from www.host.com on non-secure
#   port 189:
#      listen ldap://www.host.com:189
#
#   Listen for client requests from www.host2.com on the default secure
#   port 636:
#      listen ldaps://www.host2.com
#
#   Listen for client requests from www.host2.com on secure port 136:
#      listen ldaps://www.host2.com:136
#
#   Listen for client requests from IPv4 address 192.168.0.100 on the
#   default non-secure port 389:
#      listen ldap://192.168.0.100
#
#   Listen for client requests from IPv4 address 192.168.0.100 on
#   non-secure port 289:
#      listen ldap://192.168.0.100:289
#
#   Listen for client requests from IPv4 address 192.168.1.100 on
#   secure port 236:
#      listen ldaps://192.168.1.100:236
#
#   Listen for client requests from IPv6 address 1080::8:800:200C:417A
#   on the default non-secure port 389:
#      listen ldap://[1080::8:800:200C:417A]
#
#   Listen for client requests from IPv6 address 1080::8:800:200C:417B
#   on non-secure port 489:
#      listen ldap://[1080::8:800:200C:417B]:489
#
#   Listen for client requests from IPv6 address 1080::8:800:200C:417C
#   on secure port 436:
#      listen ldaps://[1080::8:800:200C:417C]:436
#
# Notes:
#   Only one LDAP server on each system can listen for requests using
#   the Program Call interface.  The listen option overrides any values
#   specified by the deprecated options; security, port and securePort.
#----------------------------------------------------------------------
listen ldap://:389
#listen ldap://:pc

#----------------------------------------------------------------------
# logFile <filename>
#
# Default Value: /etc/ldap/gldlog.output
#
# Description:
#   The logFile option specifies the location of the file where the
#   activity log is written when logging is enabled.
#
# Example:
#   logFile /tmp/ldap/gldlog.output
#
# Notes:
#   File names can be specified in one of the following ways:
#    /pathname/filename
#      Specifies the full path name of a file in the z/OS UNIX System
#      Services file system to store log records.
#    filename
#      Specifies a file name to store log records in the z/OS UNIX
#      System Services file system that is relative to the current
#      working directory of the LDAP server.  Note that when running
#      from a started task or batch, there is no current working
#      directory defined. This format is not recommended.
#    //'dataset.name'
#      Specifies the fully-qualified name of an allocated sequential
#      dataset to store log records.
#    //'dataset.name(member)'
#      Specifies the fully-qualified name of an allocated partitioned
#      dataset and member to store log records.
#    //DD:DDNAME
#      Specifies the DDNAME to store log records.
#----------------------------------------------------------------------
#logFile /etc/ldap/gldlog.output

#----------------------------------------------------------------------
# logFileFilter <filter>
#
# Default Value: ibm-filterIP=*
#
# Description:
#   The logFileFilter option specifies a client IP address filter used
#   to determine the activity that is included or excluded from being
#   logged in the activity log file.  Client requests originating from
#   IP addresses allowed by the filter are written to the activity log
#   file specified in the logFile configuration option.
#
#   The only supported activity log filters are ones using the
#   ibm-filterIP attribute type to designate the client IPv4 addresses
#   or IPv6 addresses with no brackets that are to be included or
#   excluded from the activity log file.  Hostnames and subnet masks
#   are not supported in these filters.
#
# Example:
#   logFileFilter (|(ibm-filterIP=192.1.1.*)(ibm-filterIP=193.1.2.*))
#----------------------------------------------------------------------
#logFileFilter ibm-filterIP=*

#----------------------------------------------------------------------
# logFileMicroseconds <on | off>
#
# Default Value: off
#
# Description:
#   The logFileMicroseconds option controls whether all generated
#   activity log records contain microseconds in their time stamps.
#
# Example:
#   logFileMicroseconds off
#
# Notes:
#   The GLDLOG_MICROSECONDS environment variable which can also control
#   this behavior is now deprecated.  If both the keyword and the
#   environment variable are set, the keyword setting will be used.
#----------------------------------------------------------------------
#logFileMicroseconds off

#----------------------------------------------------------------------
# logFileMsg <msgs | noMsgs>
#
# Default Value: noMsgs
#
# Description:
#   The logFileMsg option controls if activity log records are
#   generated when the LDAP server processes requests.  Messages are
#   not written to the log if the option is not specified.
#
# Example:
#   logFileMsg noMsgs
#
# Notes:
#   The GLDLOG_MSG environment variable which can also control
#   this behavior is now deprecated.  If both the keyword and the
#   environment variable are set, the keyword setting will be used.
#----------------------------------------------------------------------
#logFileMsg noMsgs

#----------------------------------------------------------------------
# logFileOps <writeOps | allOps | summary | noOps>
#
# Default Value: noOps
#
# Description:
#   The logFileOps option controls which operations generate LDAP server
#   activity log records.  No operations are logged if the option is set
#   to noOps or not specified.
#
# Example:
#   logFileOps noOps
#
# Notes:
#   The GLDLOG_OPS environment variable which can also control
#   this behavior is now deprecated.  If both the keyword and the
#   environment variable are set, the keyword setting will be used.
#----------------------------------------------------------------------
#logFileOps noOps

#----------------------------------------------------------------------
# logFileRecordType <both | begin | mergedRecord>
#
# Default Value: begin
#
# Description:
#   The logFileRecordType option controls when the LDAP server
#   generates activity log records.  Begin, signifies the record is
#   generated at the beginning of the request.  Both, signifies the
#   record is generated at both beginning and end.  MergedRecord,
#   signifies information from both records is merged into a single
#   record at the end.  Log records are not generated when an operation
#   ends if this option is not specified or is set to begin.
#
# Example:
#   logFileRecordType begin
#
# Notes:
#   The GLDLOG_TIME environment variable which can also control
#   this behavior is now deprecated.  If both the keyword and the
#   environment variable are set, the keyword setting will be used.
#----------------------------------------------------------------------
#logFileRecordType begin

#----------------------------------------------------------------------
# logFileRolloverDirectory <path>
#
# Default Value: Directory specified by the logFile option
#
# Description:
#   The logFileRolloverDirectory option specifies the name of the z/OS
#   UNIX System Services file system directory where the activity log
#   files are archived or rolled over or the Generated Data Group (GDG)
#   base dataset.  If a z/OS UNIX System Services file system directory
#   is specified, it must be a fully-qualified directory path.  This
#   option is ignored if a sequential or partitioned dataset is
#   specified for the logFile configuration option.  If the logFile
#   configuration option specifies a file that resides in a z/OS UNIX
#   System Services file system directory and this option is not
#   specified, the archived or rolled over activity log file is kept in
#   the same directory.
#
# Example:
#   logFileRolloverDirectory /etc/tmp/logs/
#----------------------------------------------------------------------
#logFileRolloverDirectory /etc/ldap/

#----------------------------------------------------------------------
# logFileRolloverSize <integer-number>[K|M|G]
#
# Default Value: 0
#
# Description:
#   The logFileRolloverSize option specifies the maximum size in bytes
#   of the activity log file.  When the maximum size is reached, the
#   activity log file is rolled over or archived.  The value can be
#   followed by a K, M, or G to indicate kilobytes, megabytes, or
#   gigabytes, respectively and must be at least 10K(10240) but no
#   larger than:
#   - 18446744073709551615
#   - 18014398509481983K
#   - 17592186044415M
#   - 17179869183G
#
#   Specify 0 to disable activity log file archiving or roll over based
#   on size. See Activity logging for more information about activity
#   log archiving or roll over.
#
# Example:
#   logFileRolloverSize 2G
#----------------------------------------------------------------------
#logFileRolloverSize 0

#----------------------------------------------------------------------
# logFileRolloverTOD <hh:mm>
#
# Default Value: 24:00
#
# Description:
#   The logFileRolloverTOD option specifies the time of day when the
#   activity log file is archived or rolled over.  Every day at the
#   specified time, the current activity log file is rolled over or
#   archived.  The value must be between 00:00 and 23:59.  Specify a
#   value outside this range to disable activity log file archiving or
#   roll over based on time of day.
#
# Example:
#   logFileRolloverTOD 13:00
#----------------------------------------------------------------------
#logFileRolloverTOD 24:00

#----------------------------------------------------------------------
# logFileVersion <0 | 1>
#
# Default Value: 0
#
# Description:
#   The logFileVersion option specifies the activity log version.  Log
#   file version 1 enables connect, disconnect and abandon activity log
#   records.
#
# Example:
#   logFileVersion 0
#----------------------------------------------------------------------
#logFileVersion 0

#----------------------------------------------------------------------
# logFileSync <integer-count>
#
# Default Value: 0
#
# Description:
#   The logFileSync option specifies the log file synchronization
#   interval in seconds.  The value must be 0 or between 30 and 600.
#   A value of 0 disables asynchronous activity logging and records
#   client activities without cache.
#
# Example:
#   logFileSync 0
#----------------------------------------------------------------------
#logFileSync 0

#----------------------------------------------------------------------
# maxConnections <integer-count>
#
# Default Value: operating system maximum
#
# Description:
#   The maxConnections option specifies the maximum number of
#   concurrently connected clients that the LDAP server allows.
#
# Example:
#   maxConnections 200
#
# Notes:
#   The range is 30 to 65535.
#----------------------------------------------------------------------
maxConnections 65535

#----------------------------------------------------------------------
# operationsMonitor <ip | ipAny | all>
#
# Default Value: ipAny
#
# Description:
#   The operationsMonitor option specifies the search patterns
#   monitored by the LDAP server.  If operationsMonitor is enabled,
#   the LDAP server monitors search statistics for the types of search
#   patterns that are configured.
#
# Example:
#   operationsMonitor all
#
# Notes:
#   This option is ignored and no operations monitoring is done if
#   operations monitor is turned off by specifying a value of 0 for
#   the operationsMonitorSize option.
#----------------------------------------------------------------------
#operationsMonitor ipAny

#----------------------------------------------------------------------
# operationsMonitorSize <integer-size>
#
# Default Value: 1000
#
# Description:
#   The operationsMonitorSize option specifies the maximum number of
#   search patterns for which the operations monitor gathers
#   statistics.
#
# Example:
#   operationsMonitorSize 5000
#
# Notes:
#   The maximum size for this value is 2147483647.  A value of 0
#   turns the operations monitor off.  When the operations monitor
#   is turned off, the cn=operations,cn=monitor entry is not
#   returned on a cn=monitor search.
#----------------------------------------------------------------------
#operationsMonitorSize 1000

#----------------------------------------------------------------------
# pcIdleConnectionTimeout <integer-timeout>
#
# Default Value: indefinite if not specified
#
# Description:
#   The pcIdleConnectionTimeout option specifies the amount of time in
#   seconds that an idle connection will remain valid over the LDAP PC
#   (Program Call) callable interface.  After the specified time, the
#   PC connection is considered no longer in use and any resources
#   associated with the connection are released.
#
# Example:
#   pcIdleConnectionTimeout 300
#----------------------------------------------------------------------
#pcIdleConnectionTimeout 60

#----------------------------------------------------------------------
# pcThreads <integer-count>
#
# Default Value: 10
#
# Description:
#   The pcThreads option specifies the number of threads to handle
#   incoming program call requests using the z/OS SAF interface to the
#   LDAP server.
#
# Example:
#   pcThreads 3
#
# Notes:
#   The minimum value is 2.  No threads will be used if the program
#   call interface is not active.
#----------------------------------------------------------------------
#pcThreads 10

#----------------------------------------------------------------------
# pwSearchOutput <binary | base64>
#
# Description:
#   The PWSEARCHOUTPUT option specifies the format of MD5 and SHA
#   hashed userPassword and ibm-slapdAdminPw attribute values when
#   retrieved on a search operation.  This option does not affect the
#   retrieval of Salted SHA (SSHA), SHA-2, or Salted SHA-2 hashed
#   userPassword and ibm-slapdAdminPw attribute values on a search
#   operation.
#
#   If set to binary and a userPassword or ibm-slapdAdminPw attribute
#   value is hashed in MD5 or SHA, the LDAP server returns the
#   encryption tag (either {MD5} or {SHA}) in UTF-8 followed by the
#   binary hash.
#
#   If set to base64 and a userPassword or ibm-slapdAdminPw attribute
#   value is encrypted in MD5 or SHA, the LDAP server returns the
#   encryption tag (either {MD5} or {SHA}) in UTF-8 followed by the
#   base64-encoded binary hash.
#
# Default Value: binary
#
# Example:
#   pwSearchOutput base64
#----------------------------------------------------------------------
#pwSearchOutput binary

#----------------------------------------------------------------------
# referral <ldap-url>
#
# Description:
#   The referral option specifies the referral to pass back when the
#   target of a client request is not included in any suffix within
#   the LDAP server.  It is also known as the default referral.
#
# Notes:
#   This option can be specified multiple times.
#
# Example:
#   referral ldap://referral.server.com:3389
#----------------------------------------------------------------------
#referral ldap://referral.server.com:3389

#----------------------------------------------------------------------
# schemaPath <path>
#
# Default Value: /var/ldap/schema
#
# Description:
#   The schemaPath option specifies the name of the file system
#   directory containing the LDAP schema database and checkpoint files.
#   A fully qualified directory path must be specified.
#
# Example:
#   schemaPath /home/myLDAP/schema
#
# Notes:
#   The schema database file is automatically created during LDAP
#   server initialization if it does not already exist.  The LDAP
#   server must have write access to the schema directory.
#----------------------------------------------------------------------
#schemaPath /var/ldap/schema

#----------------------------------------------------------------------
# schemaReplaceByValue <on | off>
#
# Default Value: on
#
# Description:
#   The schemaReplaceByValue option determines the behavior of modify
#   operations with replace values of the schema entry.
#
# Example:
#   schemaReplaceByValue off
#
# Notes:
#   When off is specified, a modify operation with replace values for
#   an attribute in the schema entry behaves like a 'normal' modify
#   operation: all the values currently in the attribute are replaced
#   by the values specified in the modify operation.
#
#   When on is specified, individual values of the attribute in the
#   schema entry can be replaced without removing all the other values
#   currently in the attribute.
#----------------------------------------------------------------------
#schemaReplaceByValue on

#----------------------------------------------------------------------
# securityLabel <on | off>
#
# Default Value: off
#
# Description:
#   The securityLabel option determines whether or not security label
#   processing is activated with bound LDAP clients.
#
# Example:
#   securityLabel on
#
# Notes:
#   When set to on, the security labels associated with the LDAP client
#   and LDAP server are verified during the authentication process.
#   Security labels are recorded in all LDAP audit records.
#----------------------------------------------------------------------
#securityLabel off

#----------------------------------------------------------------------
# sendV3StringsOverV2As <UTF-8 | ISO8859-1>
#
# Default Value: UTF-8
#
# Description:
#   The sendV3StringsOverV2As option specifies the data format to use
#   when sending and receiving textual information over the LDAP
#   Version 2 protocol.
#
# Example:
#   sendV3StringsOverV2As UTF-8
#----------------------------------------------------------------------
sendV3StringsOverV2As UTF-8

#----------------------------------------------------------------------
# serverCompatLevel < 3 | 4 | 5 | 6 | 7 | 8 >
#
# Default Value: 8 if not running in a sysplex
#                4 if running in a sysplex
#
# Description:
#   The serverCompatLevel option specifies the server compatibility
#   level.  This value can be used to limit the functions supported by
#   the server so that the server can be compatible with older versions
#   of LDAP servers when they are sharing directory data in a sysplex
#   group.  To produce consistent results, all the LDAP servers in the
#   same sysplex group name must support the same functions.  Refer to
#   <IBM Tivoli Directory Server Administration and Use> for more
#   detail what each server compat level indicates.
#
# Example:
#   serverCompatLevel = 4
#----------------------------------------------------------------------
#serverCompatLevel 8

#----------------------------------------------------------------------
# serverEtherAddr <mac-address>
#
# Default Value: The LDAP server uses the hardware model and serial
#                numbers to generate a MAC address.
#
# Description:
#   The serverEtherAddr option specifies the Media Access Control (MAC)
#   address used for entry UUID generation.  You must specify the MAC
#   address if multiple LDAP servers will run on a (hardware) system.
#   This applies if your LDAP servers are on different LPARs and also
#   if two LDAP servers are on the same LPAR.  You do not need to
#   specify this field if this is the only LDAP server that will run
#   on this (hardware) system.
#
# Example:
#   serverEtherAddr 402064040959
#
# Notes:
#   This value must be unique for all LDAP servers in your enterprise.
#   The MAC address consists of 12 hexadecimal digits.
#----------------------------------------------------------------------
#serverEtherAddr

#----------------------------------------------------------------------
# serverSysplexGroup <name>
#
# Description:
#   The serverSysplexGroup option specifies that this LDAP server is
#   participating in data sharing in a sysplex and indicates the name
#   of the cross-system coupling facility (XCF) group.  All LDAP
#   servers in the sysplex that specify the same group name share the
#   LDAP server schema and the directories of backends that specify the
#   multiserver on option.
#----------------------------------------------------------------------
#serverSysplexGroup

#----------------------------------------------------------------------
# sizeLimit <integer-number>
#
# Default Value: 500
#
# Description:
#   The sizeLimit option specifies the maximum number of entries to
#   return for a search operation.  The number must be between 0 and
#   2147483647.
#
# Example:
#   sizeLimit 350
#----------------------------------------------------------------------
sizeLimit 500

#----------------------------------------------------------------------
# srvStartUpError <terminate | ignore>
#
# Default Value: terminate
#
# Description:
#   The srvStartUpError option specifies whether the LDAP server stops
#   if a backend fails to initialize, after the configuration file is
#   read.  If terminate is specified, the server ends when any backend
#   fails to initialize.  If ignore is specified, the LDAP server
#   continues processing as long as the schema successfully
#   initializes.  The option also apples to a failure when
#   initializing the LDAP PC callable support interface, when
#   configured.
#
# Example:
#   srvStartUpError ignore
#
# Notes:
#   A configuration error that occurs before backend initialization
#   begins always causes the server to stop.
#----------------------------------------------------------------------
#srvStartUpError terminate

#----------------------------------------------------------------------
# tcpTerminate <terminate | recover>
#
# Default Value: recover
#
# Description:
#   The tcpTerminate option specifies whether the LDAP server ends
#   when network interfaces are not active.  The LDAP server
#   periodically polls the network interfaces it is using to determine
#   when they go down and come back up.  If an interface fails but the
#   LDAP server still has at least one active interface, the server
#   continues processing and reestablishes a failed interface when it
#   detects that is has become active.  If all interfaces fail and
#   terminate is specified, the LDAP server stops.  If recover is
#   specified, then the LDAP server remains active and attempts to
#   reestablish network interfaces with it detects they have become
#   active.  All client operations targeted to the LDAP server fail
#   until a network interface can be reconnected.  The frequency of
#   polling can be set using the LDAP_NETWORK_POLL environment
#   variable.
#
# Example:
#   tcpTerminate terminate
#----------------------------------------------------------------------
#tcpTerminate recover

#----------------------------------------------------------------------
# timeLimit <integer-number>
#
# Default Value: 3600
#
# Description:
#   The timeLimit option specifies the maximum number of seconds the
#   backend will spend answering a search request.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   timeLimit 60
#
# Notes:
#   The default setting allows connections to stay active for 1 hour.
#----------------------------------------------------------------------
timeLimit 3600

#----------------------------------------------------------------------
# unbufferedEntryLimit <integer-number>
#
# Default Value: 2147483647
#
# Description:
#   The unbufferedEntryLimit option specifies the maximum number of
#   returned search entries that will be sent immediately over the
#   network before the LDAP server begins buffering results for an
#   individual request.  The number must be between 0 and 2147483647.
#
# Example:
#   unbufferedEntryLimit 500
#
# Notes:
#   A value of 2147483647 indicates that no search results are
#   buffered.
#   A value of 0 indicates that all search results are buffered.
#----------------------------------------------------------------------
#unbufferedEntryLimit 2147483647

#----------------------------------------------------------------------
# validateIncomingV2strings <on | off>
#
# Default Value: on
#
# Description:
#   The validateIncomingV2strings option specifies whether incoming
#   strings are validated.  If set to on, this option limits the format
#   of incoming string data sent over the LDAP Version 2 protocol to
#   the IA5 character set (X'00'-X'7F' or 7-bit ASCII).
#
# Example:
#   validateIncomingV2strings on
#----------------------------------------------------------------------
validateIncomingV2strings on

#----------------------------------------------------------------------
# wlmExcept <name> <IP_address> <dn>
#
# Description:
#   The wlmExcept option specifies the Workload Manager (WLM)
#   transaction name used for client requests originating from an IP
#   address or a bound user's distinguished name (DN).  The wlmExecpt
#   option can be specified multiple times to allow the routing of
#   different LDAP client requests to the same or different WLM
#   transaction name.  During LDAP server initialization, a WLM
#   enclave is created for each unique name.
#
# Example:
#   wlmExcept except1 1.2.3.4 cn=you,o=ibm
#   wlmExcept except2 1.2.3.4 cn=you2,o=ibm
#   wlmExcept except3 1.2.3.4
#   wlmExcept except4 cn=you,o=ibm
#
# Notes:
#   This option can be specified multiple times.
#
#   If both the IP_address and dn values are not specified with the
#   wlmExcept configuration option, a WLM enclave will be created with
#   the transaction value 'name'.  However, the enclave is not
#   associated with any client requests until a WLMEXCEPT modify
#   command is issued.
#----------------------------------------------------------------------
#wlmExcept

#----------------------------------------------------------------------
# SSL/TLS specific CONFIGURATION SETTINGS
#----------------------------------------------------------------------

#----------------------------------------------------------------------
# sslAuth <serverAuth | serverClientAuth>
#
# Default Value: serverAuth
#
# Description:
#   The sslAuth option specifies the SSL/TLS authentication method.
#
# Example:
#   sslAuth serverClientAuth
#----------------------------------------------------------------------
#sslAuth serverAuth

#----------------------------------------------------------------------
# sslCertificate <certificate-label | none>
#
# Default Value: none
#
# Description:
#   The sslCertificate option specifies the label of the certificate
#   that is used for LDAP server authentication.
#
# Example:
#   sslCertificate cert1
#
# Notes:
#   If the value is none, the default certificate is used for server
#   authentication.
#----------------------------------------------------------------------
#sslCertificate none

#----------------------------------------------------------------------
# sslCipherSpecs <cipher-specification>
#
# Default Value: GSK_V3_CIPHER_SPECS_EXPANDED
#
# Description:
#   The sslCipherSpecs option specifies the SSL Version 3 and TLS
#   Version 1 cipher suites that will be accepted from clients.  Use of
#   this option to specify the specific cipher suites is limited, and
#   provided only for backward compatibility.  It supports only a
#   portion of the cipher suites available in z/OS System SSL, contains
#   no 4-character cipher suites, and provides no order of preference.
#   The preferred approach is to set the option to
#   GSK_V3_CIPHER_SPECS_EXPANDED and then set the environment variable
#   GSK_V3_CIPHER_SPECS_EXPANDED to the desired list of 4-character
#   cipher specifications in order of preference.
#
# Examples:
#   sslCipherSpecs ANY
#   sslCipherSpecs TRIPLE_DES_SHA_US+DES_SHA_EXPORT
#   sslCipherSpecs GSK_V3_CIPHER_SPECS_EXPANDED
#----------------------------------------------------------------------
#sslCipherSpecs GSK_V3_CIPHER_SPECS_EXPANDED

#----------------------------------------------------------------------
# sslFipsState <Off | Level1 | Level2 | Level3>
#
# Default Value: Off
#
# Description:
#   The sslFipsState option specifies the FIPS state for the LDAP server.
#   When FIPS mode turned on, it is more restrictive with respect to
#   cryptographic algorithms, protocols and key sizes that can be supported.
#
# Examples:
#   sslFipsState Off
#   sslFipsState Level1
#   sslFipsState Level2
#   sslFipsState Level3
#
#----------------------------------------------------------------------
#sslFipsState Off

#----------------------------------------------------------------------
# sslKeyRingFile <filename | keyring | token>
#
# Description:
#   The sslKeyRingFile option specifies the path and file name of the
#   SSL/TLS key database file, RACF key ring name, or PKCS #11 token
#   name for the LDAP server.  SSL/TLS connections are only
#   available when this option is specified.
#
# Examples:
#   sslKeyRingFile /etc/ldap/key.kdb
#   sslKeyRingFile LDAPRING
#   sslKeyRingFile *TOKEN*/MYTOKEN
#
# Notes:
#   The sslKeyRingFilePW and sslKeyRingPWStashFile configuration
#   options must not be specified when a RACF key ring name or
#   PKCS #11 token name is specified for this option.
#----------------------------------------------------------------------
#sslKeyRingFile LDAPRING

#----------------------------------------------------------------------
# sslKeyRingFilePW <password>
#
# Description:
#   The sslKeyRingFilePW option specifies the password protecting
#   access to the SSL/TLS key database file.
#
# Example:
#   sslKeyRingFilePW password
#
# Notes:
#   Use of this option is strongly discouraged.  As an alternative,
#   use RACF key ring support, PKCS #11 support, or the
#   sslKeyRingPWStashFile configuration option.  This will eliminate
#   this password from the configuration file.
#----------------------------------------------------------------------
#sslKeyRingFilePW password

#----------------------------------------------------------------------
# sslKeyRingPWStashFile <filename>
#
# Description:
#   The sslKeyRingPWStashFile option specifies a file system file name
#   where the password for the server's key database file is stashed.
#
# Example:
#   sslKeyRingPWStashFile /etc/ldap/key.sth
#
# Notes:
#   If this option is present, then the password from this stash file
#   overrides the sslKeyRingFilePW configuration option, if present.
#   Use the gskkyman utility with the -s option to create a key
#   database password stash file.
#----------------------------------------------------------------------
#sslKeyRingPWStashFile /etc/ldap/key.sth

#----------------------------------------------------------------------
# sslMapCertificate <off|check|add|replace> <fail|ignore>
#
# Description:
#   The sslMapCertificate option specifies whether the server maps a
#   certificate used in a SASL EXTERNAL bind to the RACF user that is
#   associated with the certificate.
#
# Default Value: off fail
#
# Example:
#   sslMapCertificate check fail
#
# Notes:
#   When off is specified for the first value, RACF is not searched
#   for the user ID associated with the certificate and no certificate
#   mapping is performed.  In this case, it does not matter what the
#   second value is (fail or ignore).
#----------------------------------------------------------------------
#sslMapCertificate off fail

#----------------------------------------------------------------------
# Digest authentication CONFIGURATION SETTINGS
#----------------------------------------------------------------------

#----------------------------------------------------------------------
# digestRealm <hostname>
#
# Default Value: primary host name obtained from DNS
#
# Description:
#   The digestRealm option specifies a realm name to be used when doing
#   DIGEST-MD5 or CRAM-MD5 SASL authentication binds.
#
# Example:
#   digestRealm host.server.com
#
# Notes:
#   This value is sent to the client to help hash the password while
#   doing a DIGEST-MD5 or CRAM-MD5 SASL bind.
#----------------------------------------------------------------------
#digestRealm host.server.com

#----------------------------------------------------------------------
# Kerberos specific CONFIGURATION SETTINGS
#----------------------------------------------------------------------

#----------------------------------------------------------------------
# supportKrb5 <on | off>
#
# Default Value: off
#
# Description:
#   The supportKrb5 option specifies if the LDAP server accepts
#   Kerberos GSS-API authentication for SASL bind requests.
#
# Example:
#   supportKrb5 on
#----------------------------------------------------------------------
#supportKrb5 off

#----------------------------------------------------------------------
# krbKeytab <server-keytab | none>
#
# Default Value: none
#
# Description:
#   The krbKeytab option specifies the Kerberos key table that will be
#   used by the LDAP server.  The key table is used to obtain the
#   encryption key for the Kerberos principal associated with the LDAP
#   server.
#
# Examples:
#   krbKeytab /etc/ldap/ldapkrbkeytab
#   krbKeytab LDAPKRBKEYTAB
#
# Notes:
#   A key table must be provided if Kerberos authentication is used and
#   the Kerberos KDC is not running on the same system as the LDAP
#   server.  However, a key table is not necessary if the Kerberos KDC
#   is running on the same system as the LDAP server, the user ID
#   associated with the LDAP server has a RACF KERB segment containing
#   the server principal name and the user ID associated with the LDAP
#   server has read permission to the IRR.RUSERMAP facility class.  In
#   this case, the krbKeytab option should either be omitted or set to
#   none.
#----------------------------------------------------------------------
#krbKeytab none

#----------------------------------------------------------------------
# krbLDAPAdmin <dn>
#
# Description:
#   The krbLDAPAdmin option specifies the Kerberos identity that
#   represents the LDAP administrator.
#
# Example:
#   krbLDAPAdmin ibm-kn=adminprincipal@realm-name
#
# Notes:
#   This option allows the administrator to bind using Kerberos
#   authentication and still maintain administrative authority.  The
#   value for this option must be specified as a DN with the attribute
#   type ibm-kn.  The ibm-kn attribute type is case-sensitive and must
#   match the actual Kerberos identity.
#----------------------------------------------------------------------
#krbLDAPAdmin ibm-kn=adminprincipal@realm-name

#----------------------------------------------------------------------
# serverKrbPrinc <kerberos identity>
#
# Description:
#   The serverKrbPrinc option specifies the Kerberos principal name
#   assigned to the LDAP server.  This value becomes the server name in
#   Kerberos service tickets.
#
# Example:
#   serverKrbPrinc ldap/primary-dns-hostname@default-krbRealmName
#
# Notes:
#   The primary-dns-hostname must be the canonical host name returned
#   by the DNS name service.
#----------------------------------------------------------------------
#serverKrbPrinc ldap/primary-host-name@realm-name

#----------------------------------------------------------------------
# Plug-in specific CONFIGURATION SETTINGS
#----------------------------------------------------------------------

#----------------------------------------------------------------------
# plugin <type dllName initFunction "pluginParms">
#
# Description:
#   The plugin option defines a plug-in extension to the LDAP server.
#      type - specify preOperation, clientOperation or postOperation
#      dllName - specify the file name of the shared library (DLL)
#                containing the plug-in code
#      initFunction - specify the name of the plug-in entry point
#                     initialization routine
#      pluginParms - optionally, specify plug-in parameters, all
#                    pluginParms must be within double quotes,
#                    and separated by blank characters.
#
# Example:
#   plugin preOperation PreOpPlugDLL preOp_init "list of parms"
#
# Notes:
#   The plug-in can retrieve the pluginParms using the
#   slapi_pblock_get() routine.  Multiple plugin statements may be
#   specified.
#----------------------------------------------------------------------
#plugin


# =====================================================================
# =====================================================================
# =====================================================================
# =====================================================================
#
# DATABASE-SPECIFIC SECTIONS
#
# =====================================================================
# =====================================================================
# =====================================================================
# =====================================================================


#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------

# SDBM-specific CONFIGURATION SETTINGS

#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------


#----------------------------------------------------------------------
# database dbtype dblibpath
#
# Description:
#   The database option marks the beginning of a new database section.
#
# Example:
#   database SDBM GLDBSD31/GLDBSD64
#
# Notes:
#   All global options must appear before the first database section.
#   An optional name may be specified to identify this backend.
#----------------------------------------------------------------------
#database SDBM GLDBSD31/GLDBSD64

#----------------------------------------------------------------------
# include <filename>
#
# Description:
#   The include option will cause the specified file to be included
#   as part of the LDAP server configuration.
#
# Example:
#   include //'sys1.sdbm.parms'
#----------------------------------------------------------------------
#include "//'sys1.sdbm.parms'"

#----------------------------------------------------------------------
# suffix <dn-suffix>
#
# Description:
#   The suffix option specifies the root of a subtree in the namespace
#   managed by this server within this backend.
#
# Example:
#   suffix "sysplex=sysplex1"
#
# Notes:
#   This option is required when using the SDBM backend.
#----------------------------------------------------------------------
#suffix "sysplex=sysplex1"

#----------------------------------------------------------------------
# enableResources <on | off>
#
# Default Value: off
#
# Description:
#   The enableResources option specifies whether the SDBM backend
#   supports operations on RACF resources and classes.  If on, SDBM
#   accepts operations for the setropts, class, and resource profile
#   entries.  LDAP also accepts requests for creating a change log
#   entry for a change to a RACF resource profile.  If off, an SDBM
#   search from the suffix does not return these entries and
#   operations (including a change log request) involving these
#   entries are rejected.
#
# Example:
#   enableResources on
#----------------------------------------------------------------------
#enableResources off

#----------------------------------------------------------------------
# extendedSearch <off | basic | advanced>
#
# Default Value: off
#
# Description:
#   The extendedSearch option controls the SDBM backend search filters
#   and 4096-line output restriction.
#   If set to off, search requests are processed in traditional mode
#   which supports limited search filters and is subject to the RACF
#   R_admin 4096-line output restriction.
#   If set to basic, search requests are processed with basic extended
#   search which includes limited search filters and removes the
#   4096-line output restriction.
#   If set to advanced, search requests are processed with advanced
#   extended search which supports all search filters in addition to
#   the removal of the 4096-line output restriction.
#
# Example:
#   extendedSearch off
#----------------------------------------------------------------------
#extendedSearch off

#----------------------------------------------------------------------
# krbIdentityMap <on | off>
#
# Default Value: off
#
# Description:
#   The krbIdentityMap option specifies if this backend will
#   participate in Kerberos identity mapping.  If this option is on,
#   the LDAP server will attempt to map the Kerberos identity that
#   performed the bind to DNs that exist in this backend.  The mapped
#   DNs will then be used for access control.
#
# Example:
#   krbIdentityMap on
#----------------------------------------------------------------------
#krbIdentityMap off

#----------------------------------------------------------------------
# readOnly <on | off>
#
# Default Value: off
#
# Description:
#   The readOnly option specifies whether or not this database can be
#   modified.
#
# Example:
#   readOnly on
#----------------------------------------------------------------------
#readOnly off

#----------------------------------------------------------------------
# sizeLimit <integer-number>
#
# Default Value: 500
#
# Description:
#   The sizeLimit option specifies the maximum number of entries in
#   this backend, to return for a search operation.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   sizeLimit 350
#----------------------------------------------------------------------
#sizeLimit 500

#----------------------------------------------------------------------
# timeLimit <integer-number>
#
# Default Value: 3600
#
# Description:
#   The timeLimit option specifies the maximum number of seconds this
#   backend will spend answering a search request.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   timeLimit 60
#
# Notes:
#   The default setting allows connections to stay active for 1 hour.
#----------------------------------------------------------------------
#timeLimit 3600

#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------

# TDBM-specific CONFIGURATION SETTINGS

#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------


#----------------------------------------------------------------------
# database dbtype dblibpath
#
# Description:
#   The database option marks the beginning of a new database section.
#
# Example:
#   database TDBM GLDBTD31/GLDBTD64
#
# Notes:
#   All global options must appear before the first database section.
#   An optional name may be specified to identify this backend.
#----------------------------------------------------------------------
#database TDBM GLDBTD31/GLDBTD64

#----------------------------------------------------------------------
# include <filename>
#
# Description:
#   The include option will cause the specified file to be included
#   as part of the LDAP server configuration.
#
# Example:
#   include //'sys1.tdbm.parms'
#----------------------------------------------------------------------
#include "//'sys1.tdbm.parms'"

#----------------------------------------------------------------------
# suffix <dn-suffix>
#
# Description:
#   The suffix option specifies the root of a subtree in the namespace
#   managed by this server within this backend.
#
# Example:
#   suffix "o=Your Company"
#
# Notes:
#   This option is required when using the TDBM backend.
#   This option can be specified multiple times.
#----------------------------------------------------------------------
#suffix "o=Your Company"

#----------------------------------------------------------------------
# aclSourceCacheSize <integer-size>
#
# Default Value: 100
#
# Description:
#   The aclSourceCacheSize option specifies the maximum number of
#   entries to store in the ACL source cache.  The cache holds
#   information about ACL definitions within the database.  Retrieval
#   of information from this cache avoids database read operations when
#   resolving access permissions.
#
# Example:
#   aclSourceCacheSize 10
#
# Notes:
#   A value of 0 indicates that the cache should not be used.
#----------------------------------------------------------------------
#aclSourceCacheSize 10

#----------------------------------------------------------------------
# attrOverflowCount <integer-size>
#
# Default Value: 512
#
# Description:
#   The attrOverflowCount option specifies the number of attribute
#   values required to store the value in a long attribute value table.
#   The choice of this value allows large multi-valued attributes such
#   as group membership lists to be stored in a separate table with its
#   own index.
#
# Example:
#   attrOverflowCount 1000
#
# Notes:
#   A value of 0 will disable attribute overflow based on the attribute
#   value count.
#----------------------------------------------------------------------
#attrOverflowCount 512

#----------------------------------------------------------------------
# attrOverflowSize <integer-size>
#
# Default Value: 255
#
# Description:
#   The attrOverflowSize option specifies, in bytes, the minimum size
#   of an attribute value required to store the value in a long
#   attribute value table.  The choice of this value allows large
#   attribute values, such as JPEG and GIF files, to be stored in a
#   separate DB2 table in a separate DB2 table space.
#
# Example:
#   attrOverflowSize 10000
#
# Notes:
#   A value of 0 will disable attribute overflow based on the attribute
#   size.
#----------------------------------------------------------------------
#attrOverflowSize 255

#----------------------------------------------------------------------
# changeLoggingParticipant <on | off>
#
# Default Value: on
#
# Description:
#   The changeLoggingParticipant option allows or disallows change
#   logging for changes made to entries in this backend.
#
# Example:
#   changeLoggingParticipant off
#----------------------------------------------------------------------
#changeLoggingParticipant on

#----------------------------------------------------------------------
# db2CheckHealth <on | off>
#
# Description:
#   The db2CheckHealth option specifies if the backend participates
#   in the DB2 health check if it is enabled on the LDAP server.
#
# Example:
#   db2CheckHealth on
#----------------------------------------------------------------------
#db2CheckHealth off

#----------------------------------------------------------------------
# db2ExtentLimit <integer-value>
#
# Description:
#   The db2ExtentLimit option specifies the ExtentLimit parameter of
#   DSNACCOX stored procedure used by DB2 health check to define the
#   number of physical extents.
#
# Example:
#   db2ExtentLimit 254
#----------------------------------------------------------------------
#db2ExtentLimit 254

#----------------------------------------------------------------------
# db2RRIAppendInsertPct <double-value>
#
# Description:
#   The db2RRIAppendInsertPct option specifies the RRIAppendInsertPct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the number of index entries that were inserted
#   with a key value greater than the maximum key value in the index
#   space or partition to the number of index entries.
#
# Example:
#   db2RRIAppendInsertPct 20.0
#----------------------------------------------------------------------
#db2RRIAppendInsertPct 20.0

#----------------------------------------------------------------------
# db2RRIDeletesAbs <integer-value>
#
# Description:
#   The db2RRIDeletesAbs option specifies the RRIDeletesAbs parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   sum of the number of index entries that were deleted.
#
# Example:
#   db2RRIDeletesAbs 0
#----------------------------------------------------------------------
#db2RRIDeletesAbs 0

#----------------------------------------------------------------------
# db2RRIDeletesPct <double-value>
#
# Description:
#   The db2RRIDeletesPct option specifies the RRIDeletesPct parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the sum of the number of index entries that were deleted
#   to the total number of index entries.
#
# Example:
#   db2RRIDeletesPct 30.0
#----------------------------------------------------------------------
#db2RRIDeletesPct 30.0

#----------------------------------------------------------------------
# db2RRIInsertsAbs <integer-value>
#
# Description:
#   The db2RRIInsertsAbs option specifies the RRIInsertsAbs parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   sum of the number of index entries that were inserted.
#
# Example:
#   db2RRIInsertsAbs 0
#----------------------------------------------------------------------
#db2RRIInsertsAbs 0

#----------------------------------------------------------------------
# db2RRIInsertsPct <double-value>
#
# Description:
#   The db2RRIInsertsPct option specifies the RRIInsertsPct parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the sum of the number of index entries that were inserted
#   to the total number of index entries.
#
# Example:
#   db2RRIInsertsPct 30.0
#----------------------------------------------------------------------
#db2RRIInsertsPct 30.0

#----------------------------------------------------------------------
# db2RRILeafLimit <double-value>
#
# Description:
#   The db2RRILeafLimit option specifies the RRILeafLimit parameter of
#   DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the number of index page splits in which the higher part
#   of the split page was far from the location of the original page to
#   the total number of active pages.
#
# Example:
#   db2RRILeafLimit 10.0
#----------------------------------------------------------------------
#db2RRILeafLimit 10.0

#----------------------------------------------------------------------
# db2RRIMassDelLimit <integer-value>
#
# Description:
#   The db2RRIMassDelLimit option specifies the RRIMassDelLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes of index entries.
#
# Example:
#   db2RRIMassDelLimit 0
#----------------------------------------------------------------------
#db2RRIMassDelLimit 0

#----------------------------------------------------------------------
# db2RRINumLevelsLimit <integer-value>
#
# Description:
#   The db2RRINumLevelsLimit option specifies the RRINumLevelsLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes of index entries.
#
# Example:
#   db2RRINumLevelsLimit 0
#----------------------------------------------------------------------
#db2RRINumLevelsLimit 0

#----------------------------------------------------------------------
# db2RRIPseudoDeletePct <double-value>
#
# Description:
#   The db2RRIPseudoDeletePct option specifies the RRIPseudoDeletePct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the number of index entries that were
#   pseudo-deleted to the number of index entries.
#
# Example:
#   db2RRIPseudoDeletePct 10.0
#----------------------------------------------------------------------
#db2RRIPseudoDeletePct 10.0

#----------------------------------------------------------------------
# db2RRTDataSpaceRat <double-value>
#
# Description:
#   The db2RRTDataSpaceRat option specifies the RRTDataSpaceRat
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the space allocated to the actual space used.
#
# Example:
#   db2RRTDataSpaceRat -1.0
#----------------------------------------------------------------------
#db2RRTDataSpaceRat -1.0

#----------------------------------------------------------------------
# db2RRTDeletesAbs <integer-value>
#
# Description:
#   The db2RRTDeletesAbs option specifies the RRTDeletesAbs parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   total number of delete operations.
#
# Example:
#   db2RRTDeletesAbs 0
#----------------------------------------------------------------------
#db2RRTDeletesAbs 0

#----------------------------------------------------------------------
# db2RRTDeletesPct <double-value>
#
# Description:
#   The db2RRTDeletesPct option specifies the RRTDeletesPct parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the total number of delete operations to the total number
#   of rows.
#
# Example:
#   db2RRTDeletesPct 25.0
#----------------------------------------------------------------------
#db2RRTDeletesPct 25.0

#----------------------------------------------------------------------
# db2RRTDisorgLOBPct <double-value>
#
# Description:
#   The db2RRTDisorgLOBPct option specifies the RRTDisorgLOBPct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the  ratio of the number of imperfectly chunked LOBs to the
#   total number of rows.
#
# Example:
#   db2RRTDisorgLOBPct 50.0
#----------------------------------------------------------------------
#db2RRTDisorgLOBPct 50.0

#----------------------------------------------------------------------
# db2RRTIndRefLimit <double-value>
#
# Description:
#   The db2RRTIndRefLimit option specifies the RRTIndRefLimit parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the total number of overflow records that were created to
#   the total number of rows.
#
# Example:
#   db2RRTIndRefLimit 10.0
#----------------------------------------------------------------------
#db2RRTIndRefLimit 10.0

#----------------------------------------------------------------------
# db2RRTInsertsAbs <integer-value>
#
# Description:
#   The db2RRTInsertsAbs option specifies the RRTInsertsAbs parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   total number of insert operations.
#
# Example:
#   db2RRTInsertsAbs 0
#----------------------------------------------------------------------
#db2RRTInsertsAbs 0

#----------------------------------------------------------------------
# db2RRTInsertsPct <double-value>
#
# Description:
#   The db2RRTInsertsPct option specifies the RRTInsertsPct parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the total number of insert operations to the total number
#   of rows.
#
# Example:
#   db2RRTInsertsPct 25.0
#----------------------------------------------------------------------
#db2RRTInsertsPct 25.0

#----------------------------------------------------------------------
# db2RRTMassDelLimit <integer-value>
#
# Description:
#   The db2RRTMassDelLimit option specifies the RRTMassDelLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes.
#
# Example:
#   db2RRTMassDelLimit 0
#----------------------------------------------------------------------
#db2RRTMassDelLimit 0

#----------------------------------------------------------------------
# db2RRTUnclustInsPct <double-value>
#
# Description:
#   The db2RRTUnclustInsPct option specifies the RRTUnclustInsPct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the total number of unclustered insert
#   operations to the total number of rows.
#
# Example:
#   db2RRTUnclustInsPct 10.0
#----------------------------------------------------------------------
#db2RRTUnclustInsPct 10.0

#----------------------------------------------------------------------
# db2SRIInsDelAbs <integer-value>
#
# Description:
#   The db2SRIInsDelAbs option specifies the SRIInsDelAbs parameter of
#   DSNACCOX stored procedure used by DB2 health check to define the
#   total number of inserted and deleted index entries.
#
# Example:
#   db2SRIInsDelAbs 0
#----------------------------------------------------------------------
#db2SRIInsDelAbs 0

#----------------------------------------------------------------------
# db2SRIInsDelPct <double-value>
#
# Description:
#   The db2SRIInsDelPct option specifies the SRIInsDelPct parameter of
#   DSNACCOX stored procedure used by DB2 health check todefine the
#   ratio of the total number of unclustered insert operations to the
#   total number of rows.
#
# Example:
#   db2SRIInsDelPct 20.0
#----------------------------------------------------------------------
#db2SRIInsDelPct 20.0

#----------------------------------------------------------------------
# db2SRIMassDelLimit <integer-value>
#
# Description:
#   The db2SRIMassDelLimit option specifies the SRIMassDelLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes.
#
# Example:
#   db2SRIMassDelLimit 0
#----------------------------------------------------------------------
#db2SRIMassDelLimit 0

#----------------------------------------------------------------------
# db2SRTInsDelUpdAbs <integer-value>
#
# Description:
#   The db2SRTInsDelUpdAbs option specifies the SRTInsDelUpdAbs
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the number of insert, update, and delete operations.
#
# Example:
#   db2SRTInsDelUpdAbs 0
#----------------------------------------------------------------------
#db2SRTInsDelUpdAbs 0

#----------------------------------------------------------------------
# db2SRTInsDelUpdPct <double-value>
#
# Description:
#   The db2SRTInsDelUpdPct option specifies the SRTInsDelUpdPct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the total number of insert, update, or delete
#   operations to the total number of rows.
#
# Example:
#   db2SRTInsDelUpdPct 20.0
#----------------------------------------------------------------------
#db2SRTInsDelUpdPct 20.0

#----------------------------------------------------------------------
# db2SRTMassDelLimit <integer-value>
#
# Description:
#   The db2SRTMassDelLimit option specifies the SRTMassDelLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes.
#
# Example:
#   db2SRTMassDelLimit 0
#----------------------------------------------------------------------
#db2SRTMassDelLimit 0

#----------------------------------------------------------------------
# dbuserid <userid>
#
# Description:
#   The dbuserid option specifies the z/OS user ID that will be the
#   owner of the DB2 tables.
#
# Example:
#   dbuserid LDAPSRV
#
# Notes:
#   This option is required when using the TDBM backend.  This value
#   must be unique within this configuration file.  Multiple backends
#   on an LDAP server cannot share a database.
#----------------------------------------------------------------------
#dbuserid GLDSRV

#----------------------------------------------------------------------
# dnToEidCacheSize <integer>
#
# Default Value: 1000
#
# Description:
#   The dnToEidCacheSize option specifies the maximum number of entries
#   in the distinguished name to entry identifier mapping cache.
#   This cache is used to map the distinguished name for a directory
#   entry to the associated entry identifier for that entry.  Retrieval
#   of information from this cache avoids database read operations
#   while locating entries in the database.
#
# Example:
#   dnToEidCacheSize 5000
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#dnToEidCacheSize 1000

#----------------------------------------------------------------------
# dsnaoini <dsname>
#
# Description:
#   The dsnaoini option specifies the name of the DB2 CLI
#   initialization file or sequential data set (or PDS member).
#
# Example:
#   dsnaoini /etc/ldap/db2cli.ini
#
# Notes:
#   The dsnaoini option must be the same for all DB2-based backends.
#----------------------------------------------------------------------
#dsnaoini GLD.CNFOUT(DSNAOINI)

#----------------------------------------------------------------------
# entryCacheSize <integer>
#
# Default Value: 5000
#
# Description:
#   The entryCacheSize option specifies the maximum number of entries
#   in the directory entry cache.  This cache holds information
#   contained within individual directory entries.  Retrieval of
#   information from this cache avoids database read operations when
#   processing entries within the database.
#
# Example:
#   entryCacheSize 2000
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#entryCacheSize 5000

#----------------------------------------------------------------------
# entryOwnerCacheSize <integer>
#
# Default Value: 100
#
# Description:
#   The entryOwnerCacheSize option specifies the maximum number of
#   entries in the entry owner cache.  This cache holds information
#   about ACL definitions.  Retrieval of information from this cache
#   avoids database read operations when resolving access permissions.
#
# Example:
#   entryOwnerCacheSize 500
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#entryOwnerCacheSize 100

#----------------------------------------------------------------------
# extendedGroupSearching <on | off>
#
# Default Value: off
#
# Description:
#   The extendedGroupSearching option specifies whether this backend
#   participates in extended group membership searching on a client
#   bind request.  If this option is on, group memberships are gathered
#   from this backend during LDAP bind processing in addition to the
#   backend in which the bind DN resides. If this option is off, group
#   memberships are not gathered from this backend unless the bind DN
#   resides in this backend.
#
# Example:
#   extendedGroupSearching on
#----------------------------------------------------------------------
#extendedGroupSearching off

#----------------------------------------------------------------------
# fetchSize <integer>
#
# Default Value: 65535
#
# Description:
#   The fetchSize option specifies the maximum buffer size in bytes
#   that is internally converted to the number of rows to fetch
#   each time if multi-row fetch is applicable.
#
# Example:
#   fetchSize 65535
#
# Notes:
#   A non-positive value indicates multi-row fetch will not be used.
#----------------------------------------------------------------------
#fetchSize 65535

#----------------------------------------------------------------------
# filterCacheBypassLimit <integer>
#
# Default Value: 100
#
# Description:
#   The filterCacheBypassLimit option specifies the maximum number of
#   returned entries allowed in the result set of any individual search
#   that will be stored in the search filter cache.  Search filters
#   that match more than this number of entries will not be added to
#   the search filter cache.  This option is useful for maintaining
#   the effectiveness of the search filter cache and the entry cache.
#   It can be used to prevent a few search requests with larger result
#   sets from dominating the contents of the entry cache.
#
# Example:
#   filterCacheBypassLimit 200
#
# Notes:
#   This value is not used when the filter cache is not in use.
#----------------------------------------------------------------------
#filterCacheBypassLimit 100

#----------------------------------------------------------------------
# filterCacheSize <integer>
#
# Default Value: 5000
#
# Description:
#   The filterCacheSize option specifies the maximum number of filters
#   in the search filter cache.  This cache holds information related
#   to the mapping of search request to result sets.  Retrieval of
#   information from this cache avoids database read operations when
#   processing search requests.  Search requests which return more
#   entries than specified by the filterCacheBypassLimit configuration
#   option are not placed in the search filter cache.
#
# Example:
#   filterCacheSize 10000
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#filterCacheSize 5000

#----------------------------------------------------------------------
# krbIdentityMap <on | off>
#
# Default Value: off
#
# Description:
#   The krbIdentityMap option specifies if this backend will
#   participate in Kerberos identity mapping.  If this option is on,
#   the LDAP server will attempt to map the Kerberos identity that
#   performed the bind to DNs that exist in this backend.  The mapped
#   DNs will then be used for access control.
#
# Example:
#   krbIdentityMap on
#----------------------------------------------------------------------
#krbIdentityMap off

#----------------------------------------------------------------------
# masterServer <ldap-url>
#
# Description:
#   The masterServer option specifies the location of this replica's
#   master server, for this backend, for basic replication.  The
#   presence of this option indicates that this LDAP server is a basic
#   replication read-only replica for this backend and receives updates
#   from a master LDAP server.  Any other update requests for this
#   backend received directly by the this LDAP server is redirected to
#   the master server.
#
# Example:
#   masterServer ldap://tdbmMaster.server.com:3389
#
# Notes:
#   The masterServerDN option must also be specified in this section of
#   the configuration file.  The masterServer option indicates basic
#   replication is configured for this backend section.  The
#   masterServer option cannot be specified if the
#   useAdvancedReplication option is set to 'on' in the CDBM backend
#   database section.
#----------------------------------------------------------------------
#masterServer ldap://tdbmMaster.server.com:3389

#----------------------------------------------------------------------
# masterServerDN <dn>
#
# Description:
#   The masterServerDN option specifies the distinguished name (DN)
#   always allowed to make changes to this basic replication read-only
#   replica backend.  The presence of this option indicates that this
#   LDAP server is a read-only replica for this backend and receives
#   updates from a master LDAP server using the specified DN.  The
#   specified DN is a special entry that should only be used when
#   replicating to this read-only replica backend.  The DN has
#   unrestricted update, compare, and search access for all entries in
#   the backend on this server, even if the LDAP server is in
#   maintenance mode.  When in maintenance mode, only this DN and the
#   LDAP administrator can access and update the entries in this
#   backend.  All other update operations for this backend received by
#   the replica server are redirected to the master server.  Care must
#   be taken when updating this backend to ensure the replica server
#   remains synchronized with the master server.
#
# Example:
#   masterServerDN "cn=Master Server, o=Your Company"
#
# Notes:
#   You must also specify the masterServer option in this section of
#   the configuration file.  You cannot specify the peerServerDN
#   option.
#
#   The masterServerDN option indicates basic replication is configured
#   for this backend section.  Therefore, the masterServerDN option
#   cannot be specified if the useAdvancedReplication option is set to
#   'on' in the CDBM backend database section
#----------------------------------------------------------------------
#masterServerDN "cn=Master Server, o=Your Company"

#----------------------------------------------------------------------
# masterServerPW <password>
#
# Description:
#   The masterServerPW option specifies the password for the
#   masterServerDN that is allowed to make updates for this backend.
#   This option is only applicable for a basic replication read-only
#   LDAP server.
#
# Example:
#   masterServerPW mPassword
#
# Notes:
#   Use of the masterServerPW configuration option is strongly
#   discouraged in production environments.  Instead, specify your
#   masterServerDN as the distinguished name of an existing entry in
#   the directory information tree, including a userPassword attribute.
#   This will eliminate passwords from the configuration file.
#
#   The masterServerPW option indicates basic replication is configured
#   for this backend section.  Therefore, the masterServerPW option
#   cannot be specified if the useAdvancedReplication option is set to
#   'on' in the CDBM backend database section
#----------------------------------------------------------------------
#masterServerPW secret

#----------------------------------------------------------------------
# multiserver <on | off>
#
# Default Value: off
#
# Description:
#   The multiserver option specifies the operating mode for this
#   backend.
#
# Example:
#   multiserver on
#
# Notes:
#   You can configure a backend to operate in single-server mode while
#   another backend operates in multi-server mode except when GDBM or
#   CDBM is configured.  When CDBM or GDBM is configured, all TDBM,
#   LDBM, GDBM, and CDBM backends must be configured to use the same
#   operating mode.
#----------------------------------------------------------------------
#multiserver off

#----------------------------------------------------------------------
# nativeAuthSubtree <dn | all>
#
# Default Value: all
#
# Description:
#   The nativeAuthSubtree option specifies the distinguished name of a
#   subtree where all of its entries are eligible to participate in
#   native authentication.
#
# Examples:
#   nativeAuthSubtree "ou=pok,o=IBM,c=US"
#   nativeAuthSubtree "ou=endicott,o=IBM,c=US"
#
# Notes:
#   The entire directory is subject to native authentication when this
#   option is set to all.  The useNativeAuth option is used to enable
#   or disable native authentication.
#----------------------------------------------------------------------
#nativeAuthSubtree all

#----------------------------------------------------------------------
# nativeUpdateAllowed <on | off | reset>
#
# Default Value: off
#
# Description:
#   When set to on or reset, the nativeUpdateAllowed option enables
#   native password or password phrase changes in the Security Server
#   to occur through a modify request to the TDBM backend if the
#   useNativeAuth selected or all option is specified.
#
#   When set to reset, this option also allows a bind to the backend to
#   succeed even if the specified native authentication password is
#   expired, as long as the PasswordPolicy control is included in the
#   bind request. After the bind, only the special delete-add
#   modification of the bound user's userPassword attribute can be
#   performed to reset the native authentication password. Once that is
#   done, other LDAP operations can be performed.
#
# Example:
#   nativeUpdateAllowed on
#
# Notes:
#   This option does not affect the ability to change a native
#   password or password phrase during a bind operation.
#----------------------------------------------------------------------
#nativeUpdateAllowed off

#----------------------------------------------------------------------
# peerServerDN <dn>
#
# Description:
#   The peerServerDN option specifies the distinguished name (DN)
#   always allowed to make changes to this basic replication peer
#   replica backend.  The presence of this option indicates that this
#   LDAP server is a peer replica for this backend, and can receive
#   updates from another peer LDAP server using the specified DN as
#   well as processing updates received from clients.  The specified DN
#   is a special entry that should only be used when replicating to
#   this peer replica backend.  The DN has unrestricted update,
#   compare, and search access for all entries in the backend on this
#   server, even if the LDAP server is in maintenance mode.  When in
#   maintenance mode, only this DN and the LDAP administrator can
#   access and update the entries in this backend.
#
# Example:
#   peerServerDN "cn=Peer Server, o=Your Company"
#
# Notes:
#   You cannot specify the masterServerDN option in this section of the
#   configuration file.
#
#   The peerServerDN option indicates basic peer-to-peer replication is
#   configured for this backend section.  Therefore, the peerServerDN
#   option cannot be specified if the useAdvancedReplication option is
#   set to 'on' in the CDBM backend database section.
#----------------------------------------------------------------------
#peerServerDN "cn=Peer Server, o=Your Company"

#----------------------------------------------------------------------
# peerServerPW <password>
#
# Description:
#   The peerServerPW option specifies the password for the
#   peerServerDN that is allowed to make updates for this backend.
#   This option is only applicable for a basic replication peer
#   replica LDAP server.
#
# Example:
#   peerServerPW pPassword
#
# Notes:
#   Use of the peerServerPW configuration option is strongly
#   discouraged in production environments.  Instead, specify your
#   peerServerDN as the distinguished name of an existing entry in the
#   directory information tree, including a userPassword attribute.
#   This will eliminate passwords from the configuration file.
#
#   The peerServerPW option indicates basic peer-to-peer replication
#   is configured for this backend section.  Therefore, the
#   peerServerPW option cannot be specified if the
#   useAdvancedReplication option is set to 'on' in the CDBM backend
#   database section.
#----------------------------------------------------------------------
#peerServerPW secret

#----------------------------------------------------------------------
# persistentSearch <on | off>
#
# Default Value: off
#
# Description:
#   The persistentSearch option specifies whether this backend supports
#   the persistent search operation.
#
# Example:
#   persistentSearch on
#----------------------------------------------------------------------
#persistentSearch off

#----------------------------------------------------------------------
# pwCryptCompat <on | off>
#
# Default Value: on
#
# Description:
#   The pwCryptCompat option specifies whether to use an EBCDIC
#   version or a UTF-8 version of the crypt() algorithm, to encrypt
#   passwords when the crypt value is used for the pwEncryption option.
#   When set to on, the EBCDIC version of the crypt() algorithm is used
#   as the encryption method.  When set to off, the UTF-8 version is
#   used.
#
# Example:
#   pwCryptCompat off
#
# Notes:
#   ASCII is a subset of UTF-8.  When sharing LDAP directory data
#   between z/OS and an ASCII-based platform, specify off to ensure
#   that the encrypted value is the same on both platforms.
#----------------------------------------------------------------------
#pwCryptCompat on

#----------------------------------------------------------------------
# pwEncryption < none | crypt | MD5 | SHA | SSHA | SHA224 |
#                SSHA224 | SHA256 | SSHA256 | SHA384 | SSHA384 |
#                SHA512 | SSHA512 | DES:keylabel | AES:keylabel >
#
# Default Value: none
#
# Description:
#   The pwEncryption option specifies the encryption or hashing method
#   to use when storing the userPassword and ibm-slapdAdminPw attribute
#   values in this backend.
#
# Example:
#   pwEncryption crypt
#----------------------------------------------------------------------
#pwEncryption none

#----------------------------------------------------------------------
# readOnly <on | off>
#
# Default Value: off
#
# Description:
#   The readOnly option specifies whether or not this database can be
#   modified.
#
# Example:
#   readOnly on
#
# Notes:
#   Do not use this option in replica servers as this will not allow
#   replica updates, sent from the master server.
#----------------------------------------------------------------------
#readOnly off

#----------------------------------------------------------------------
# secretEncryption <none | DES:keylabel | AES:keylabel>
#
# Default Value: none
#
# Description:
#   The secretEncryption option specifies the encryption method to use
#   when storing the secretKey, replicaCredentials, ibm-replicaKeyPwd,
#   and ibm-slapdMasterPw attribute values in this backend.
#
# Example:
#   secretEncryption DES:deslabel
#
# Notes:
#   Applications may use the secretKey attribute type to store
#   sensitive data that needs to be encrypted in the directory and to
#   retrieve the data in clear text format.  This encryption method is
#   used to protect the replicaCredentials attribute values in this
#   backend when basic replication is enabled.  This encryption method
#   also protects the ibm-replicaKeyPwd and ibm-slapdMasterPw attribute
#   values in this backend when advanced replication is enabled.
#----------------------------------------------------------------------
#secretEncryption none

#----------------------------------------------------------------------
# serverName <name>
#
# Description:
#   The serverName option specifies the name of the DB2 server location
#   that manages the tables for the LDAP server.
#
# Example:
#   serverName LOC1
#
# Notes:
#   This value must match the name of one of the DATA SOURCE stanzas in
#   the ODBC initialization data set specified by the dsnaoini option.
#   This name is also referred to as the location, in some DB2
#   documentation.  The serverName option must be the same for all TDBM
#   and DB2-based GDBM backends.
#----------------------------------------------------------------------
#serverName LOC1

#----------------------------------------------------------------------
# sizeLimit <integer-number>
#
# Default Value: 500
#
# Description:
#   The sizeLimit option specifies the maximum number of entries in
#   this backend, to return for a search operation.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   sizeLimit 350
#----------------------------------------------------------------------
#sizeLimit 500

#----------------------------------------------------------------------
# timeLimit <integer-number>
#
# Default Value: 3600
#
# Description:
#   The timeLimit option specifies the maximum number of seconds this
#   backend will spend answering a search request.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   timeLimit 60
#
# Notes:
#   The default setting allows connections to stay active for 1 hour.
#----------------------------------------------------------------------
#timeLimit 3600

#----------------------------------------------------------------------
# useNativeAuth <selected | all | off>
#
# Default Value: off
#
# Description:
#   The useNativeAuth option specifies if the LDAP server will use
#   native authentication for this backend.
#
# Example:
#   useNativeAuth all
#
# Notes:
#   Use the following value when:
#     selected - only entries with the ibm-nativeId attribute that are
#                within subtrees (as specified by the
#                nativeAuthSubtree option) use native authentication
#     all      - all entries within native subtrees (as specified by
#                the nativeAuthSubtree option) use native
#                authentication; these entries can contain either the
#                ibm-nativeId or uid attribute to specify the RACF ID.
#     off      - no entries participate in native authentication
#----------------------------------------------------------------------
#useNativeAuth off


#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#
# DB2-based GDBM-specific CONFIGURATION SETTINGS
#
# NOTE: The GDBM backend can be configured in one of two mutually
#       exclusive usages, either as a file-based change log backend
#       or as a DB2-based change log backend.  This file includes
#       a separate section for each use.
#
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------


#----------------------------------------------------------------------
# database dbtype dblibpath
#
# Description:
#   The database option marks the beginning of a new database section.
#
# Example:
#   database GDBM GLDBGD31/GLDBGD64
#
# Notes:
#   All global options must appear before the first database section.
#   An optional name may be specified to identify this backend.
#----------------------------------------------------------------------
#database GDBM GLDBGD31/GLDBGD64

#----------------------------------------------------------------------
# include <filename>
#
# Default Value:
#
# Description:
#   The include option will cause the specified file to be included
#   as part of the LDAP server configuration.
#
# Example:
#   include //'sys1.gdbm.parms'
#----------------------------------------------------------------------
#include "//'sys1.gdbmDB2.parms'"

#----------------------------------------------------------------------
# aclSourceCacheSize <integer-size>
#
# Default Value: 100
#
# Description:
#   The aclSourceCacheSize option specifies the maximum number of
#   entries to store in the ACL source cache.  The cache holds
#   information about ACL definitions within the database.  Retrieval
#   of information from this cache avoids database read operations when
#   resolving access permissions.
#
# Example:
#   aclSourceCacheSize 10
#
# Notes:
#   A value of 0 indicates that the cache should not be used.
#----------------------------------------------------------------------
#aclSourceCacheSize 10

#----------------------------------------------------------------------
# attrOverflowSize <integer-size>
#
# Default Value: 255
#
# Description:
#   The attrOverflowSize option specifies, in bytes, the minimum size
#   of an attribute value required to store the value in a long
#   attribute value table.  The choice of this value allows large
#   attribute values, such as JPEG and GIF files, to be stored in a
#   separate DB2 table in a separate DB2 table space.
#
# Example:
#   attrOverflowSize 10000
#
# Notes:
#   A value of 0 will disable attribute overflow based on the attribute
#   size.
#----------------------------------------------------------------------
#attrOverflowSize 255

#----------------------------------------------------------------------
# changeLogging <on | off>
#
# Default Value: on
#
# Description:
#   The changeLogging option turns change logging on or off.  While
#   change logging is on, all change logging operations are allowed.
#   When change logging is off, change log entries can be searched,
#   modified and deleted, but no new change log entries can be created
#   and no automatic trimming of the change log is performed.
#
# Example:
#   changeLogging off
#----------------------------------------------------------------------
#changeLogging on

#----------------------------------------------------------------------
# changeLoggingParticipant <on | off>
#
# Default Value: on
#
# Description:
#   The changeLoggingParticipant option controls the logging of
#   modifications to the LDAP server schema entry.
#
# Example:
#   changeLoggingParticipant off
#
# Notes:
#   For this backend, this option does not turn on or off change
#   logging.  That is done by the changeLogging option.
#----------------------------------------------------------------------
#changeLoggingParticipant on

#----------------------------------------------------------------------
# changeLogMaxAge <integer>
#
# Default Value: 0
#
# Description:
#   The changeLogMaxAge option specifies the maximum age in seconds of
#   an entry in the change log.  Change log entries are deleted when
#   they have been in the change log longer that this value, except
#   if changeLogging off is specified.
#
# Example:
#   changeLogMaxAge 3600
#
# Notes:
#   A value of 0 indicates that there is no maximum age.
#----------------------------------------------------------------------
#changeLogMaxAge 0

#----------------------------------------------------------------------
# changeLogMaxEntries <integer>
#
# Default Value: 0
#
# Description:
#   The changeLogMaxEntries option specifies the maximum number of
#   entries that the change log can contain.  If the number of change
#   log entries exceeds this value and changeLogging on is specified,
#   change log entries with the lowest change numbers are deleted.
#
# Example:
#   changeLogMaxEntries 10000
#
# Notes:
#   A value of 0 indicates that there is no maximum number of entries
#   that the change log can contain.
#
#   For this backend, change log entries are deleted until the number
#   of remaining entries is 95% of the maximum.
#----------------------------------------------------------------------
#changeLogMaxEntries 0

#----------------------------------------------------------------------
# db2CheckHealth <on | off>
#
# Description:
#   The db2CheckHealth option specifies if the backend participates
#   in the DB2 health check if it is enabled on the LDAP server.
#
# Example:
#   db2CheckHealth on
#----------------------------------------------------------------------
#db2CheckHealth off

#----------------------------------------------------------------------
# db2ExtentLimit <integer-value>
#
# Description:
#   The db2ExtentLimit option specifies the ExtentLimit parameter of
#   DSNACCOX stored procedure used by DB2 health check to define the
#   number of physical extents.
#
# Example:
#   db2ExtentLimit 254
#----------------------------------------------------------------------
#db2ExtentLimit 254

#----------------------------------------------------------------------
# db2RRIAppendInsertPct <double-value>
#
# Description:
#   The db2RRIAppendInsertPct option specifies the RRIAppendInsertPct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the number of index entries that were inserted
#   with a key value greater than the maximum key value in the index
#   space or partition to the number of index entries.
#
# Example:
#   db2RRIAppendInsertPct 20.0
#----------------------------------------------------------------------
#db2RRIAppendInsertPct 20.0

#----------------------------------------------------------------------
# db2RRIDeletesAbs <integer-value>
#
# Description:
#   The db2RRIDeletesAbs option specifies the RRIDeletesAbs parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   sum of the number of index entries that were deleted.
#
# Example:
#   db2RRIDeletesAbs 0
#----------------------------------------------------------------------
#db2RRIDeletesAbs 0

#----------------------------------------------------------------------
# db2RRIDeletesPct <double-value>
#
# Description:
#   The db2RRIDeletesPct option specifies the RRIDeletesPct parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the sum of the number of index entries that were deleted
#   to the total number of index entries.
#
# Example:
#   db2RRIDeletesPct 30.0
#----------------------------------------------------------------------
#db2RRIDeletesPct 30.0

#----------------------------------------------------------------------
# db2RRIInsertsAbs <integer-value>
#
# Description:
#   The db2RRIInsertsAbs option specifies the RRIInsertsAbs parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   sum of the number of index entries that were inserted.
#
# Example:
#   db2RRIInsertsAbs 0
#----------------------------------------------------------------------
#db2RRIInsertsAbs 0

#----------------------------------------------------------------------
# db2RRIInsertsPct <double-value>
#
# Description:
#   The db2RRIInsertsPct option specifies the RRIInsertsPct parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the sum of the number of index entries that were inserted
#   to the total number of index entries.
#
# Example:
#   db2RRIInsertsPct 30.0
#----------------------------------------------------------------------
#db2RRIInsertsPct 30.0

#----------------------------------------------------------------------
# db2RRILeafLimit <double-value>
#
# Description:
#   The db2RRILeafLimit option specifies the RRILeafLimit parameter of
#   DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the number of index page splits in which the higher part
#   of the split page was far from the location of the original page to
#   the total number of active pages.
#
# Example:
#   db2RRILeafLimit 10.0
#----------------------------------------------------------------------
#db2RRILeafLimit 10.0

#----------------------------------------------------------------------
# db2RRIMassDelLimit <integer-value>
#
# Description:
#   The db2RRIMassDelLimit option specifies the RRIMassDelLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes of index entries.
#
# Example:
#   db2RRIMassDelLimit 0
#----------------------------------------------------------------------
#db2RRIMassDelLimit 0

#----------------------------------------------------------------------
# db2RRINumLevelsLimit <integer-value>
#
# Description:
#   The db2RRINumLevelsLimit option specifies the RRINumLevelsLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes of index entries.
#
# Example:
#   db2RRINumLevelsLimit 0
#----------------------------------------------------------------------
#db2RRINumLevelsLimit 0

#----------------------------------------------------------------------
# db2RRIPseudoDeletePct <double-value>
#
# Description:
#   The db2RRIPseudoDeletePct option specifies the RRIPseudoDeletePct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the number of index entries that were
#   pseudo-deleted to the number of index entries.
#
# Example:
#   db2RRIPseudoDeletePct 10.0
#----------------------------------------------------------------------
#db2RRIPseudoDeletePct 10.0

#----------------------------------------------------------------------
# db2RRTDataSpaceRat <double-value>
#
# Description:
#   The db2RRTDataSpaceRat option specifies the RRTDataSpaceRat
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the space allocated to the actual space used.
#
# Example:
#   db2RRTDataSpaceRat -1.0
#----------------------------------------------------------------------
#db2RRTDataSpaceRat -1.0

#----------------------------------------------------------------------
# db2RRTDeletesAbs <integer-value>
#
# Description:
#   The db2RRTDeletesAbs option specifies the RRTDeletesAbs parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   total number of delete operations.
#
# Example:
#   db2RRTDeletesAbs 0
#----------------------------------------------------------------------
#db2RRTDeletesAbs 0

#----------------------------------------------------------------------
# db2RRTDeletesPct <double-value>
#
# Description:
#   The db2RRTDeletesPct option specifies the RRTDeletesPct parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the total number of delete operations to the total number
#   of rows.
#
# Example:
#   db2RRTDeletesPct 25.0
#----------------------------------------------------------------------
#db2RRTDeletesPct 25.0

#----------------------------------------------------------------------
# db2RRTDisorgLOBPct <double-value>
#
# Description:
#   The db2RRTDisorgLOBPct option specifies the RRTDisorgLOBPct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the  ratio of the number of imperfectly chunked LOBs to the
#   total number of rows.
#
# Example:
#   db2RRTDisorgLOBPct 50.0
#----------------------------------------------------------------------
#db2RRTDisorgLOBPct 50.0

#----------------------------------------------------------------------
# db2RRTIndRefLimit <double-value>
#
# Description:
#   The db2RRTIndRefLimit option specifies the RRTIndRefLimit parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the total number of overflow records that were created to
#   the total number of rows.
#
# Example:
#   db2RRTIndRefLimit 10.0
#----------------------------------------------------------------------
#db2RRTIndRefLimit 10.0

#----------------------------------------------------------------------
# db2RRTInsertsAbs <integer-value>
#
# Description:
#   The db2RRTInsertsAbs option specifies the RRTInsertsAbs parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   total number of insert operations.
#
# Example:
#   db2RRTInsertsAbs 0
#----------------------------------------------------------------------
#db2RRTInsertsAbs 0

#----------------------------------------------------------------------
# db2RRTInsertsPct <double-value>
#
# Description:
#   The db2RRTInsertsPct option specifies the RRTInsertsPct parameter
#   of DSNACCOX stored procedure used by DB2 health check to define the
#   ratio of the total number of insert operations to the total number
#   of rows.
#
# Example:
#   db2RRTInsertsPct 25.0
#----------------------------------------------------------------------
#db2RRTInsertsPct 25.0

#----------------------------------------------------------------------
# db2RRTMassDelLimit <integer-value>
#
# Description:
#   The db2RRTMassDelLimit option specifies the RRTMassDelLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes.
#
# Example:
#   db2RRTMassDelLimit 0
#----------------------------------------------------------------------
#db2RRTMassDelLimit 0

#----------------------------------------------------------------------
# db2RRTUnclustInsPct <double-value>
#
# Description:
#   The db2RRTUnclustInsPct option specifies the RRTUnclustInsPct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the total number of unclustered insert
#   operations to the total number of rows.
#
# Example:
#   db2RRTUnclustInsPct 10.0
#----------------------------------------------------------------------
#db2RRTUnclustInsPct 10.0

#----------------------------------------------------------------------
# db2SRIInsDelAbs <integer-value>
#
# Description:
#   The db2SRIInsDelAbs option specifies the SRIInsDelAbs parameter of
#   DSNACCOX stored procedure used by DB2 health check to define the
#   total number of inserted and deleted index entries.
#
# Example:
#   db2SRIInsDelAbs 0
#----------------------------------------------------------------------
#db2SRIInsDelAbs 0

#----------------------------------------------------------------------
# db2SRIInsDelPct <double-value>
#
# Description:
#   The db2SRIInsDelPct option specifies the SRIInsDelPct parameter of
#   DSNACCOX stored procedure used by DB2 health check todefine the
#   ratio of the total number of unclustered insert operations to the
#   total number of rows.
#
# Example:
#   db2SRIInsDelPct 20.0
#----------------------------------------------------------------------
#db2SRIInsDelPct 20.0

#----------------------------------------------------------------------
# db2SRIMassDelLimit <integer-value>
#
# Description:
#   The db2SRIMassDelLimit option specifies the SRIMassDelLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes.
#
# Example:
#   db2SRIMassDelLimit 0
#----------------------------------------------------------------------
#db2SRIMassDelLimit 0

#----------------------------------------------------------------------
# db2SRTInsDelUpdAbs <integer-value>
#
# Description:
#   The db2SRTInsDelUpdAbs option specifies the SRTInsDelUpdAbs
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the number of insert, update, and delete operations.
#
# Example:
#   db2SRTInsDelUpdAbs 0
#----------------------------------------------------------------------
#db2SRTInsDelUpdAbs 0

#----------------------------------------------------------------------
# db2SRTInsDelUpdPct <double-value>
#
# Description:
#   The db2SRTInsDelUpdPct option specifies the SRTInsDelUpdPct
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the ratio of the total number of insert, update, or delete
#   operations to the total number of rows.
#
# Example:
#   db2SRTInsDelUpdPct 20.0
#----------------------------------------------------------------------
#db2SRTInsDelUpdPct 20.0

#----------------------------------------------------------------------
# db2SRTMassDelLimit <integer-value>
#
# Description:
#   The db2SRTMassDelLimit option specifies the SRTMassDelLimit
#   parameter of DSNACCOX stored procedure used by DB2 health check to
#   define the sum of the number of mass deletes.
#
# Example:
#   db2SRTMassDelLimit 0
#----------------------------------------------------------------------
#db2SRTMassDelLimit 0

#----------------------------------------------------------------------
# dbuserid <userid>
#
# Description:
#   The dbuserid option specifies the z/OS user ID that will be the
#   owner of the DB2 tables. This option indicates that this GDBM
#   backend is DB2-based and not file-based.
#
# Example:
#   dbuserid LDAPSRV
#
# Notes:
#   This option is required when using the DB2-based GDBM backend.
#   this value must be unique within this configuration file.
#   Multiple backends on an LDAP server cannot share a database.
#----------------------------------------------------------------------
#dbuserid GLDSRVG

#----------------------------------------------------------------------
# dnToEidCacheSize <integer>
#
# Default Value: 1000
#
# Description:
#   The dnToEidCacheSize option specifies the maximum number of entries
#   in the distinguished name to entry identifier mapping cache.
#   This cache is used to map the distinguished name for a directory
#   entry to the associated entry identifier for that entry.  Retrieval
#   of information from this cache avoids database read operations
#   while locating entries in the database.
#
# Example:
#   dnToEidCacheSize 5000
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#dnToEidCacheSize 1000

#----------------------------------------------------------------------
# dsnaoini <dsname>
#
# Description:
#   The dsnaoini option specifies the name of the DB2 ODBC
#   initialization dataset or file.
#
# Example:
#   dsnaoini /etc/ldap/db2cli.ini
#
# Notes:
#   The dsnaoini option must be the same for all DB2-based GDBM.
#   backends.
#----------------------------------------------------------------------
#dsnaoini GLD.CNFOUT(DSNAOINI)

#----------------------------------------------------------------------
# entryCacheSize <integer>
#
# Default Value: 5000
#
# Description:
#   The entryCacheSize option specifies the maximum number of entries
#   in the directory entry cache.  This cache holds information
#   contained within individual directory entries.  Retrieval of
#   information from this cache avoids database read operations when
#   processing entries within the database.
#
# Example:
#   entryCacheSize 2000
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#entryCacheSize 5000

#----------------------------------------------------------------------
# entryOwnerCacheSize <integer>
#
# Default Value: 100
#
# Description:
#   The entryOwnerCacheSize option specifies the maximum number of
#   entries in the entry owner cache.  This cache holds information
#   about ACL definitions.  Retrieval of information from this cache
#   avoids database read operations when resolving access permissions.
#
# Example:
#   entryOwnerCacheSize 500
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#entryOwnerCacheSize 100

#----------------------------------------------------------------------
# fetchSize <integer>
#
# Default Value: 65535
#
# Description:
#   The fetchSize option specifies the maximum buffer size in bytes
#   that is internally converted to the number of rows to fetch
#   each time if multi-row fetch is applicable.
#
# Example:
#   fetchSize 65535
#
# Notes:
#   A non-positive value indicates multi-row fetch will not be used.
#----------------------------------------------------------------------
#fetchSize 65535

#----------------------------------------------------------------------
# filterCacheBypassLimit <integer>
#
# Default Value: 100
#
# Description:
#   The filterCacheBypassLimit option specifies the maximum number of
#   returned entries allowed in the result set of any individual search
#   that will be stored in the search filter cache.  Search filters
#   that match more than this number of entries will not be added to
#   the search filter cache.  This option is useful for maintaining
#   the effectiveness of the search filter cache and the entry cache.
#   It can be used to prevent a few search requests with larger result
#   sets from dominating the contents of the entry cache.
#
# Example:
#   filterCacheBypassLimit 200
#
# Notes:
#   This value is not used when the filter cache is not in use.
#----------------------------------------------------------------------
#filterCacheBypassLimit 100

#----------------------------------------------------------------------
# filterCacheSize <integer>
#
# Default Value: 0
#
# Description:
#   The filterCacheSize option specifies the maximum number of filters
#   in the search filter cache.  This cache holds information related
#   to the mapping of search request to result sets.  Retrieval of
#   information from this cache avoids database read operations when
#   processing search requests.  Search requests which return more
#   entries than specified by the filterCacheBypassLimit configuration
#   option are not placed in the search filter cache.
#
# Example:
#   filterCacheSize 1000
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#filterCacheSize 0

#----------------------------------------------------------------------
# multiserver <on | off>
#
# Default Value: off
#
# Description:
#   The multiserver option specifies the operating mode for this
#   backend.
#
# Example:
#   multiserver on
#
# Notes:
#   You can configure a backend to operate in single-server mode while
#   another backend operates in multi-server mode except when GDBM or
#   CDBM is configured.  When CDBM or GDBM is configured, all TDBM,
#   LDBM, GDBM, and CDBM backends must be configured to use the same
#   operating mode.
#----------------------------------------------------------------------
#multiserver off

#----------------------------------------------------------------------
# persistentSearch <on | off>
#
# Default Value: off
#
# Description:
#   The persistentSearch option specifies whether this backend supports
#   the persistent search operation.
#
# Example:
#   persistentSearch on
#----------------------------------------------------------------------
#persistentSearch off

#----------------------------------------------------------------------
# readOnly <on | off>
#
# Default Value: off
#
# Description:
#   The readOnly option specifies whether or not this database can be
#   modified.
#
# Example:
#   readOnly on
#
# NOTES:
#   For this backend, change log entries are not created and are not
#   trimmed (deleted) by the LDAP server when this option is set to on.
#----------------------------------------------------------------------
#readOnly off

#----------------------------------------------------------------------
# serverName <name>
#
# Description:
#   The serverName option specifies the name of the DB2 server location
#   that manages the tables for the LDAP server.
#
# Example:
#   serverName LOC1
#
# Notes:
#   This value must match the name of one of the DATA SOURCE stanzas in
#   the ODBC initialization data set specified by the dsnaoini option.
#   This name is also referred to as the location, in some DB2
#   documentation.  The serverName option must be the same for all TDBM
#   and DB2-based GDBM backends.
#----------------------------------------------------------------------
#serverName LOC1

#----------------------------------------------------------------------
# sizeLimit <integer-number>
#
# Default Value: 500
#
# Description:
#   The sizeLimit option specifies the maximum number of entries in
#   this backend, to return for a search operation.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   sizeLimit 350
#----------------------------------------------------------------------
#sizeLimit 500

#----------------------------------------------------------------------
# timeLimit <integer-number>
#
# Default Value: 3600
#
# Description:
#   The timeLimit option specifies the maximum number of seconds this
#   backend will spend answering a search request.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   timeLimit 60
#
# Notes:
#   The default setting allows connections to stay active for 1 hour.
#----------------------------------------------------------------------
#timeLimit 3600

#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------

# LDBM-specific CONFIGURATION SETTINGS

#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------


#----------------------------------------------------------------------
# database dbtype dblibpath
#
# Description:
#   The database option marks the beginning of a new database section.
#
# Example:
#   database LDBM GLDBLD31/GLDBLD64
#
# Notes:
#   All global options must appear before the first database section.
#   An optional name may be specified to identify this backend.
#----------------------------------------------------------------------
#database LDBM GLDBLD31/GLDBLD64

#----------------------------------------------------------------------
# include <filename>
#
# Default Value:
#
# Description:
#   The include option will cause the specified file to be included
#   as part of the LDAP server configuration.
#
# Example:
#   include //'sys1.ldbm.parms'
#----------------------------------------------------------------------
#include "//'sys1.ldbm.parms'"

#----------------------------------------------------------------------
# suffix <dn-suffix>
#
# Description:
#   The suffix option specifies the root of a subtree in the namespace
#   managed by this server within this backend.
#
# Example:
#   suffix "o=Your Company"
#
# Notes:
#   This option is required when using the LDBM backend.
#   This option can be specified multiple times.
#----------------------------------------------------------------------
#suffix "o=Your Company"

#----------------------------------------------------------------------
# attrOverflowCount <integer-size>
#
# Default Value: 512
#
# Description:
#   The attrOverflowCount option specifies the number of attribute
#   values required to store the value in an internal indexed table,
#   providing quicker access to the values of large multi-valued
#   attributes such as group membership lists.
#
# Example:
#   attrOverflowCount 1000
#
# Notes:
#   A value of 0 will disable attribute overflow based on the attribute
#   value count.
#----------------------------------------------------------------------
#attrOverflowCount 512

#----------------------------------------------------------------------
# changeLoggingParticipant <on | off>
#
# Default Value: on
#
# Description:
#   The changeLoggingParticipant option allows or disallows change
#   logging for changes made to entries in this backend.
#
# Example:
#   changeLoggingParticipant off
#----------------------------------------------------------------------
#changeLoggingParticipant on

#----------------------------------------------------------------------
# commitCheckpointEntries <integer>
#
# Default Value: 10000
#
# Description:
#   The commitCheckpointEntries option specifies the maximum number of
#   entries in the checkpoint file.  An entry is added to the
#   checkpoint file each time a directory entry is added, changed,
#   deleted or renamed.  When the maximum number is reached, the
#   entries in the checkpoint file are merged into the database file
#   and the entries are removed from the checkpoint file.
#
# Example:
#   commitCheckPointEntries 5000
#
# Notes:
#   A value of 0 indicates there is no maximum.
#----------------------------------------------------------------------
#commitCheckpointEntries 10000

#----------------------------------------------------------------------
# commitCheckpointTOD <hh:mm>
#
# Default Value: 00:00
#
# Description:
#   The commitCheckpointTOD option specifies a time of day at which the
#   checkpoint file is merged into the database file.  An entry is
#   added to the checkpoint file each time a directory entry is added,
#   changed, deleted or renamed.
#
# Example:
#   commitCheckPointTOD 10:00
#
# Notes:
#   The value must be between 00:00 and 23:59.  To disable time of day
#   checkpoint processing, specify a value outside of this range.
#----------------------------------------------------------------------
#commitCheckpointTOD 00:00

#----------------------------------------------------------------------
# databaseDirectory <name>
#
# Default Value: /var/ldap/ldbm
#
# Description:
#   The databaseDirectory option specifies the name of the file system
#   directory containing the data files used by this backend, to store
#   the directory data.
#
# Example:
#   databaseDirectory /home/myLdap/ldbmData
#
# Notes:
#   A fully-qualified directory path must be specified.  A unique file
#   system directory must be specified for each file-based backend.
#   In addition, when multi-server mode is active, the same directory
#   path must be specified for each instance of this backend within the
#   cross-system group.
#----------------------------------------------------------------------
#databaseDirectory /var/ldap/ldbm

#----------------------------------------------------------------------
# extendedGroupSearching <on | off>
#
# Default Value: off
#
# Description:
#   The extendedGroupSearching option specifies whether this backend
#   participates in extended group membership searching on a client
#   bind request.  If this option is on, group memberships are gathered
#   from this backend during LDAP bind processing in addition to the
#   backend in which the bind DN resides. If this option is off, group
#   memberships are not gathered from this backend unless the bind DN
#   resides in this backend.
#
# Example:
#   extendedGroupSearching on
#----------------------------------------------------------------------
#extendedGroupSearching off

#----------------------------------------------------------------------
# fileTerminate <terminate | recover>
#
# Default Value: recover
#
# Description:
#   The fileTerminate option specifies whether the LDAP server should
#   terminate when file system errors occur.  If terminate is specified
#   the LDAP server will terminate if any file system error is
#   detected.  Otherwise, if recover is specified, the LDAP server will
#   remain active, but this backend will go into read-only mode when a
#   file system error is detected.
#
# Example:
#   fileTerminate terminate
#----------------------------------------------------------------------
#fileTerminate recover

#----------------------------------------------------------------------
# filterCacheBypassLimit <integer>
#
# Default Value: 100
#
# Description:
#   The filterCacheBypassLimit option specifies the maximum number of
#   returned entries allowed in the result set of any individual search
#   that will be stored in the search filter cache.  Search filters
#   that match more than this number of entries will not be added to
#   the search filter cache.  This option is useful for maintaining
#   the effectiveness of the search filter cache and the entry cache.
#   It can be used to prevent a few search requests with larger result
#   sets from dominating the contents of the entry cache.
#
# Example:
#   filterCacheBypassLimit 200
#
# Notes:
#   This value is not used when the filter cache is not in use.
#----------------------------------------------------------------------
#filterCacheBypassLimit 100

#----------------------------------------------------------------------
# filterCacheSize <integer>
#
# Default Value: 5000
#
# Description:
#   The filterCacheSize option specifies the maximum number of filters
#   in the search filter cache.  This cache holds information related
#   to the mapping of search request to result sets.  Retrieval of
#   information from this cache avoids database read operations when
#   processing search requests.  Search requests which return more
#   entries than specified by the filterCacheBypassLimit configuration
#   option are not placed in the search filter cache.
#
# Example:
#   filterCacheSize 10000
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#filterCacheSize 5000

#----------------------------------------------------------------------
# krbIdentityMap <on | off>
#
# Default Value: off
#
# Description:
#   The krbIdentityMap option specifies if this backend will
#   participate in Kerberos identity mapping.  If this option is on,
#   the LDAP server will attempt to map the Kerberos identity that
#   performed the bind to DNs that exist in this backend.  The mapped
#   DNs will then be used for access control.
#
# Example:
#   krbIdentityMap on
#----------------------------------------------------------------------
#krbIdentityMap off

#----------------------------------------------------------------------
# masterServer <ldap-url>
#
# Description:
#   The masterServer option specifies the location of this replica's
#   master server, for this backend, for basic replication.  The
#   presence of this option indicates that this LDAP server is a basic
#   replication read-only replica for this backend and receives updates
#   from a master LDAP server.  Any other update requests for this
#   backend received directly by the this LDAP server is redirected to
#   the master server.
#
# Example:
#   masterServer ldap://ldbmMaster.server.com:3389
#
# Notes:
#   The masterServerDN option must also be specified in this section of
#   the configuration file.  The masterServer option indicates basic
#   replication is configured for this backend section.  The
#   masterServer option cannot be specified if the
#   useAdvancedReplication option is set to 'on' in the CDBM backend
#   database section.
#----------------------------------------------------------------------
#masterServer ldap://ldbmMaster.server.com:3389

#----------------------------------------------------------------------
# masterServerDN <dn>
#
# Description:
#   The masterServerDN option specifies the distinguished name (DN)
#   always allowed to make changes to this basic replication read-only
#   replica backend.  The presence of this option indicates that this
#   LDAP server is a read-only replica for this backend and receives
#   updates from a master LDAP server using the specified DN.  The
#   specified DN is a special entry that should only be used when
#   replicating to this read-only replica backend.  The DN has
#   unrestricted update, compare, and search access for all entries in
#   the backend on this server, even if the LDAP server is in
#   maintenance mode.  When in maintenance mode, only this DN and the
#   LDAP administrator can access and update the entries in this
#   backend.  All other update operations for this backend received by
#   the replica server are redirected to the master server.  Care must
#   be taken when updating this backend to ensure the replica server
#   remains synchronized with the master server.
#
# Example:
#   masterServerDN "cn=Master Server, o=Your Company"
#
# Notes:
#   You must also specify the masterServer option in this section of
#   the configuration file.  You cannot specify the peerServerDN
#   option.
#
#   The masterServerDN option indicates basic replication is configured
#   for this backend section.  Therefore, the masterServerDN option
#   cannot be specified if the useAdvancedReplication option is set to
#   'on' in the CDBM backend database section
#----------------------------------------------------------------------
#masterServerDN "cn=Master Server, o=Your Company"

#----------------------------------------------------------------------
# masterServerPW <password>
#
# Description:
#   The masterServerPW option specifies the password for the
#   masterServerDN that is allowed to make updates for this backend.
#   This option is only applicable for a basic replication read-only
#   LDAP server.
#
# Example:
#   masterServerPW mPassword
#
# Notes:
#   Use of the masterServerPW configuration option is strongly
#   discouraged in production environments.  Instead, specify your
#   masterServerDN as the distinguished name of an existing entry in
#   the directory information tree, including a userPassword attribute.
#   This will eliminate passwords from the configuration file.
#
#   The masterServerPW option indicates basic replication is configured
#   for this backend section.  Therefore, the masterServerPW option
#   cannot be specified if the useAdvancedReplication option is set to
#   'on' in the CDBM backend database section
#----------------------------------------------------------------------
#masterServerPW secret

#----------------------------------------------------------------------
# multiserver <on | off>
#
# Default Value: off
#
# Description:
#   The multiserver option specifies the operating mode for this
#   backend.
#
# Example:
#   multiserver on
#
# Notes:
#   You can configure a backend to operate in single-server mode while
#   another backend operates in multi-server mode except when GDBM or
#   CDBM is configured.  When CDBM or GDBM is configured, all TDBM,
#   LDBM, GDBM, and CDBM backends must be configured to use the same
#   operating mode.
#----------------------------------------------------------------------
#multiserver off

#----------------------------------------------------------------------
# nativeAuthSubtree <dn | all>
#
# Default Value: all
#
# Description:
#   The nativeAuthSubtree option specifies the distinguished name of a
#   subtree where all of its entries are eligible to participate in
#   native authentication.
#
# Examples:
#   nativeAuthSubtree "ou=pok,o=IBM,c=US"
#   nativeAuthSubtree "ou=endicott,o=IBM,c=US"
#
# Notes:
#   The entire directory is subject to native authentication when this
#   option is set to all.  The useNativeAuth option is used to enable
#   or disable native authentication.
#----------------------------------------------------------------------
#nativeAuthSubtree all

#----------------------------------------------------------------------
# nativeUpdateAllowed <on | off | reset>
#
# Default Value: off
#
# Description:
#   When set to on or reset, the nativeUpdateAllowed option enables
#   native password or password phrase changes in the Security Server
#   to occur through a modify request to the LDBM backend if the
#   useNativeAuth selected or all option is specified.
#
#   When set to reset, this option also allows a bind to the backend to
#   succeed even if the specified native authentication password is
#   expired, as long as the PasswordPolicy control is included in the
#   bind request. After the bind, only the special delete-add
#   modification of the bound user's userPassword attribute can be
#   performed to reset the native authentication password. Once that is
#   done, other LDAP operations can be performed.
#
# Example:
#   nativeUpdateAllowed on
#
# Notes:
#   This option does not affect the ability to change a native
#   password or password phrase during a bind operation.
#----------------------------------------------------------------------
#nativeUpdateAllowed off

#----------------------------------------------------------------------
# peerServerDN <dn>
#
# Description:
#   The peerServerDN option specifies the distinguished name (DN)
#   always allowed to make changes to this basic replication peer
#   replica backend.  The presence of this option indicates that this
#   LDAP server is a peer replica for this backend, and can receive
#   updates from another peer LDAP server using the specified DN as
#   well as processing updates received from clients.  The specified DN
#   is a special entry that should only be used when replicating to
#   this peer replica backend.  The DN has unrestricted update,
#   compare, and search access for all entries in the backend on this
#   server, even if the LDAP server is in maintenance mode.  When in
#   maintenance mode, only this DN and the LDAP administrator can
#   access and update the entries in this backend.
#
# Example:
#   peerServerDN "cn=Peer Server, o=Your Company"
#
# Notes:
#   You cannot specify the masterServerDN option in this section of the
#   configuration file.
#
#   The peerServerDN option indicates basic peer-to-peer replication is
#   configured for this backend section.  Therefore, the peerServerDN
#   option cannot be specified if the useAdvancedReplication option is
#   set to 'on' in the CDBM backend database section.
#----------------------------------------------------------------------
#peerServerDN "cn=Peer Server, o=Your Company"

#----------------------------------------------------------------------
# peerServerPW <password>
#
# Description:
#   The peerServerPW option specifies the password for the
#   peerServerDN that is allowed to make updates for this backend.
#   This option is only applicable for a basic replication peer
#   replica LDAP server.
#
# Example:
#   peerServerPW pPassword
#
# Notes:
#   Use of the peerServerPW configuration option is strongly
#   discouraged in production environments.  Instead, specify your
#   peerServerDN as the distinguished name of an existing entry in the
#   directory information tree, including a userPassword attribute.
#   This will eliminate passwords from the configuration file.
#
#   The peerServerPW option indicates basic peer-to-peer replication
#   is configured for this backend section.  Therefore, the
#   peerServerPW option cannot be specified if the
#   useAdvancedReplication option is set to 'on' in the CDBM backend
#   database section.
#----------------------------------------------------------------------
#peerServerPW secret

#----------------------------------------------------------------------
# persistentSearch <on | off>
#
# Default Value: off
#
# Description:
#   The persistentSearch option specifies whether this backend supports
#   the persistent search operation.
#
# Example:
#   persistentSearch on
#----------------------------------------------------------------------
#persistentSearch off

#----------------------------------------------------------------------
# pwCryptCompat <on | off>
#
# Default Value: on
#
# Description:
#   The pwCryptCompat option specifies whether to use an EBCDIC
#   version or a UTF-8 version of the crypt() algorithm, to encrypt
#   passwords when the crypt value is used for the pwEncryption option.
#   When set to on, the EBCDIC version of the crypt() algorithm is used
#   as the encryption method.  When set to off, the UTF-8 version is
#   used.
#
# Example:
#   pwCryptCompat off
#
# Notes:
#   ASCII is a subset of UTF-8.  When sharing LDAP directory data
#   between z/OS and an ASCII-based platform, specify off to ensure
#   that the encrypted value is the same on both platforms.
#----------------------------------------------------------------------
#pwCryptCompat on

#----------------------------------------------------------------------
# pwEncryption < none | crypt | MD5 | SHA | SSHA | SHA224 |
#                SSHA224 | SHA256 | SSHA256 | SHA384 | SSHA384 |
#                SHA512 | SSHA512 | DES:keylabel | AES:keylabel >
#
# Default Value: none
#
# Description:
#   The pwEncryption option specifies the encryption or hashing method
#   to use when storing the userPassword and ibm-slapdAdminPw attribute
#   values in this backend.
#
# Example:
#   pwEncryption crypt
#----------------------------------------------------------------------
#pwEncryption none

#----------------------------------------------------------------------
# readOnly <on | off>
#
# Default Value: off
#
# Description:
#   The readOnly option specifies whether or not this database can be
#   modified.
#
# Example:
#   readOnly on
#
# Notes:
#   For this backend, when running in multi-server mode, this option
#   should be the same for all of the LDAP servers in the cross-system
#   group since any LDAP server can potentially become the group owner.
#   Do not use this option in replica servers as this will not allow
#   replica updates, sent from the master server.
#----------------------------------------------------------------------
#readOnly off

#----------------------------------------------------------------------
# secretEncryption <none | DES:keylabel | AES:keylabel>
#
# Default Value: none
#
# Description:
#   The secretEncryption option specifies the encryption method to use
#   when storing the secretKey, replicaCredentials, ibm-replicaKeyPwd,
#   and ibm-slapdMasterPw attribute values in this backend.
#
# Example:
#   secretEncryption DES:deslabel
#
# Notes:
#   Applications may use the secretKey attribute type to store
#   sensitive data that needs to be encrypted in the directory and to
#   retrieve the data in clear text format.  This encryption method is
#   used to protect the replicaCredentials attribute values in this
#   backend when basic replication is enabled.  This encryption method
#   also protects the ibm-replicaKeyPwd and ibm-slapdMasterPw attribute
#   values in this backend when advanced replication is enabled.
#----------------------------------------------------------------------
#secretEncryption none

#----------------------------------------------------------------------
# sizeLimit <integer-number>
#
# Default Value: 500
#
# Description:
#   The sizeLimit option specifies the maximum number of entries in
#   this backend, to return for a search operation.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   sizeLimit 350
#----------------------------------------------------------------------
#sizeLimit 500

#----------------------------------------------------------------------
# timeLimit <integer-number>
#
# Default Value: 3600
#
# Description:
#   The timeLimit option specifies the maximum number of seconds this
#   backend will spend answering a search request.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   timeLimit 60
#
# Notes:
#   The default setting allows connections to stay active for 1 hour.
#----------------------------------------------------------------------
#timeLimit 3600

#----------------------------------------------------------------------
# useNativeAuth <selected | all | off>
#
# Default Value: off
#
# Description:
#   The useNativeAuth option specifies if the LDAP server will use
#   native authentication for this backend.
#
# Example:
#   useNativeAuth all
#
# Notes:
#   Use the following value when:
#     selected - only entries with the ibm-nativeId attribute that are
#                within subtrees (as specified by the
#                nativeAuthSubtree option) use native authentication
#     all      - all entries within native subtrees (as specified by
#                the nativeAuthSubtree option) use native
#                authentication; these entries can contain either the
#                ibm-nativeId or uid attribute to specify the RACF ID.
#                userid.
#     off      - no entries participate in native authentication
#----------------------------------------------------------------------
#useNativeAuth off


#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------

# CDBM-specific CONFIGURATION SETTINGS

#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------


#----------------------------------------------------------------------
# database dbtype dblibpath
#
# Description:
#   The database option marks the beginning of a new database section.
#
# Example:
#   database CDBM GLDBCD31/GLDBCD64
#
# Notes:
#   All global options must appear before the first database section.
#   An optional name may be specified to identify this backend.
#----------------------------------------------------------------------
#database CDBM GLDBCD31/GLDBCD64 cdbm

#----------------------------------------------------------------------
# include <filename>
#
# Default Value:
#
# Description:
#   The include option will cause the specified file to be included
#   as part of the LDAP server configuration.
#
# Example:
#   include //'sys1.cdbm.parms'
#----------------------------------------------------------------------
#include "//'sys1.cdbm.parms'"

#----------------------------------------------------------------------
# attrOverflowCount <integer-size>
#
# Default Value: 512
#
# Description:
#   The attrOverflowCount option specifies the number of attribute
#   values required to store the value in an internal indexed table,
#   providing quicker access to the values of large multi-valued
#   attributes such as group membership lists.
#
# Example:
#   attrOverflowCount 1000
#
# Notes:
#   A value of 0 will disable attribute overflow based on the attribute
#   value count.
#----------------------------------------------------------------------
#attrOverflowCount 512

#----------------------------------------------------------------------
# changeLoggingParticipant <on | off>
#
# Default Value: on
#
# Description:
#   The changeLoggingParticipant option allows or disallows change
#   logging for changes made to entries in this backend.
#
# Example:
#   changeLoggingParticipant off
#----------------------------------------------------------------------
#changeLoggingParticipant on

#----------------------------------------------------------------------
# commitCheckpointEntries <integer>
#
# Default Value: 10000
#
# Description:
#   The commitCheckpointEntries option specifies the maximum number of
#   entries in the checkpoint file.  An entry is added to the
#   checkpoint file each time a directory entry is added, changed,
#   deleted or renamed.  When the maximum number is reached, the
#   entries in the checkpoint file are merged into the database file
#   and the entries are removed from the checkpoint file.
#
# Example:
#   commitCheckPointEntries 5000
#
# Notes:
#   A value of 0 indicates there is no maximum.
#----------------------------------------------------------------------
#commitCheckpointEntries 10000

#----------------------------------------------------------------------
# commitCheckpointTOD <hh:mm>
#
# Default Value: 00:00
#
# Description:
#   The commitCheckpointTOD option specifies a time of day at which the
#   checkpoint file is merged into the database file.  An entry is
#   added to the checkpoint file each time a directory entry is added,
#   changed, deleted or renamed.
#
# Example:
#   commitCheckPointTOD 10:00
#
# Notes:
#   The value must be between 00:00 and 23:59.  To disable time of day
#   checkpoint processing, specify a value outside of this range.
#----------------------------------------------------------------------
#commitCheckpointTOD 00:00

#----------------------------------------------------------------------
# databaseDirectory <name>
#
# Default Value: the value of schemaPath
#
# Description:
#   The databaseDirectory option specifies the name of the file system
#   directory containing the data files used by this backend, to store
#   the directory data.
#
# Example:
#   databaseDirectory /home/myCDBM
#
# Notes:
#   A fully-qualified directory path must be specified.  It is
#   recommended that this option is set to the same value as
#   schemaPath.  The LDAP server will set this value to the value of
#   schemaPath when this value is not explicitly set.
#----------------------------------------------------------------------
#databaseDirectory /var/ldap/schema

#----------------------------------------------------------------------
# extendedGroupSearching <on | off>
#
# Default Value: off
#
# Description:
#   The extendedGroupSearching option specifies whether this backend
#   participates in extended group membership searching on a client
#   bind request.  If this option is on, group memberships are gathered
#   from this backend during LDAP bind processing in addition to the
#   backend in which the bind DN resides. If this option is off, group
#   memberships are not gathered from this backend unless the bind DN
#   resides in this backend.
#
# Example:
#   extendedGroupSearching on
#----------------------------------------------------------------------
#extendedGroupSearching off

#----------------------------------------------------------------------
# fileTerminate <terminate | recover>
#
# Default Value: recover
#
# Description:
#   The fileTerminate option specifies whether the LDAP server should
#   terminate when file system errors occur.  If terminate is specified
#   the LDAP server will terminate if any file system error is
#   detected.  Otherwise, if recover is specified, the LDAP server will
#   remain active, but this backend will go into read-only mode when a
#   file system error is detected.
#
# Example:
#   fileTerminate terminate
#----------------------------------------------------------------------
#fileTerminate recover

#----------------------------------------------------------------------
# filterCacheBypassLimit <integer>
#
# Default Value: 100
#
# Description:
#   The filterCacheBypassLimit option specifies the maximum number of
#   returned entries allowed in the result set of any individual search
#   that will be stored in the search filter cache.  Search filters
#   that match more than this number of entries will not be added to
#   the search filter cache.  This option is useful for maintaining
#   the effectiveness of the search filter cache and the entry cache.
#   It can be used to prevent a few search requests with larger result
#   sets from dominating the contents of the entry cache.
#
# Example:
#   filterCacheBypassLimit 200
#
# Notes:
#   This value is not used when the filter cache is not in use.
#----------------------------------------------------------------------
#filterCacheBypassLimit 100

#----------------------------------------------------------------------
# filterCacheSize <integer>
#
# Default Value: 5000
#
# Description:
#   The filterCacheSize option specifies the maximum number of filters
#   in the search filter cache.  This cache holds information related
#   to the mapping of search request to result sets.  Retrieval of
#   information from this cache avoids database read operations when
#   processing search requests.  Search requests which return more
#   entries than specified by the filterCacheBypassLimit configuration
#   option are not placed in the search filter cache.
#
# Example:
#   filterCacheSize 10000
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#filterCacheSize 5000

#----------------------------------------------------------------------
# krbIdentityMap <on | off>
#
# Default Value: off
#
# Description:
#   The krbIdentityMap option specifies if this backend will
#   participate in Kerberos identity mapping.  If this option is on,
#   the LDAP server will attempt to map the Kerberos identity that
#   performed the bind to DNs that exist in this backend.  The mapped
#   DNs will then be used for access control.
#
# Example:
#   krbIdentityMap on
#----------------------------------------------------------------------
#krbIdentityMap off

#----------------------------------------------------------------------
# masterServer <ldap-url>
#
# Description:
#   The masterServer option specifies the location of this replica's
#   master server, for this backend, for basic replication.  The
#   presence of this option indicates that this LDAP server is a basic
#   replication read-only replica for this backend and receives updates
#   from a master LDAP server.  Any other update requests for this
#   backend received directly by the this LDAP server is redirected to
#   the master server.
#
# Example:
#   masterServer ldap://cdbmMaster.server.com:3389
#
# Notes:
#   The masterServerDN option must also be specified in this section of
#   the configuration file.  The masterServer option indicates basic
#   replication is configured for this backend section.  The
#   masterServer option cannot be specified if the
#   useAdvancedReplication option is set to 'on' in the CDBM backend
#   database section.
#----------------------------------------------------------------------
#masterServer ldap://cdbmMaster.server.com:3389

#----------------------------------------------------------------------
# masterServerDN <dn>
#
# Description:
#   The masterServerDN option specifies the distinguished name (DN)
#   always allowed to make changes to this basic replication read-only
#   replica backend.  The presence of this option indicates that this
#   LDAP server is a read-only replica for this backend and receives
#   updates from a master LDAP server using the specified DN.  The
#   specified DN is a special entry that should only be used when
#   replicating to this read-only replica backend.  The DN has
#   unrestricted update, compare, and search access for all entries in
#   the backend on this server, even if the LDAP server is in
#   maintenance mode.  When in maintenance mode, only this DN and the
#   LDAP administrator can access and update the entries in this
#   backend.  All other update operations for this backend received by
#   the replica server are redirected to the master server.  Care must
#   be taken when updating this backend to ensure the replica server
#   remains synchronized with the master server.
#
# Example:
#   masterServerDN "cn=Master Server, o=Your Company"
#
# Notes:
#   You must also specify the masterServer option in this section of
#   the configuration file.  You cannot specify the peerServerDN
#   option.
#
#   The masterServerDN option indicates basic replication is configured
#   for this backend section.  Therefore, the masterServerDN option
#   cannot be specified if the useAdvancedReplication option is set to
#   'on' in the CDBM backend database section
#----------------------------------------------------------------------
#masterServerDN "cn=Master Server, o=Your Company"

#----------------------------------------------------------------------
# masterServerPW <password>
#
# Description:
#   The masterServerPW option specifies the password for the
#   masterServerDN that is allowed to make updates for this backend.
#   This option is only applicable for a basic replication read-only
#   LDAP server.
#
# Example:
#   masterServerPW mPassword
#
# Notes:
#   Use of the masterServerPW configuration option is strongly
#   discouraged in production environments.  Instead, specify your
#   masterServerDN as the distinguished name of an existing entry in
#   the directory information tree, including a userPassword attribute.
#   This will eliminate passwords from the configuration file.
#
#   The masterServerPW option indicates basic replication is configured
#   for this backend section.  Therefore, the masterServerPW option
#   cannot be specified if the useAdvancedReplication option is set to
#   'on' in the CDBM backend database section
#----------------------------------------------------------------------
#masterServerPW secret

#----------------------------------------------------------------------
# multiserver <on | off>
#
# Default Value: off
#
# Description:
#   The multiserver option specifies the operating mode for this
#   backend.
#
# Example:
#   multiserver on
#
# Notes:
#   You can configure a backend to operate in single-server mode while
#   another backend operates in multi-server mode except when GDBM or
#   CDBM is configured.  When CDBM or GDBM is configured, all TDBM,
#   LDBM, GDBM, and CDBM backends must be configured to use the same
#   operating mode.
#----------------------------------------------------------------------
#multiserver off

#----------------------------------------------------------------------
# nativeAuthSubtree <dn | all>
#
# Default Value: all
#
# Description:
#   The nativeAuthSubtree option specifies the distinguished name of a
#   subtree where all of its entries are eligible to participate in
#   native authentication.
#
# Examples:
#   nativeAuthSubtree "ou=pok,o=IBM,c=US"
#   nativeAuthSubtree "ou=endicott,o=IBM,c=US"
#
# Notes:
#   The entire directory is subject to native authentication when this
#   option is set to all.  The useNativeAuth option is used to enable
#   or disable native authentication.
#----------------------------------------------------------------------
#nativeAuthSubtree all

#----------------------------------------------------------------------
# nativeUpdateAllowed <on | off | reset>
#
# Default Value: off
#
# Description:
#   When set to on or reset, the nativeUpdateAllowed option enables
#   native password or password phrase changes in the Security Server
#   to occur through a modify request to the CDBM backend if the
#   useNativeAuth selected or all option is specified.
#
#   When set to reset, this option also allows a bind to the backend to
#   succeed even if the specified native authentication password is
#   expired, as long as the PasswordPolicy control is included in the
#   bind request. After the bind, only the special delete-add
#   modification of the bound user's userPassword attribute can be
#   performed to reset the native authentication password. Once that is
#   done, other LDAP operations can be performed.
#
# Example:
#   nativeUpdateAllowed on
#
# Notes:
#   This option does not affect the ability to change a native
#   password or password phrase during a bind operation.
#----------------------------------------------------------------------
#nativeUpdateAllowed off

#----------------------------------------------------------------------
# peerServerDN <dn>
#
# Description:
#   The peerServerDN option specifies the distinguished name (DN)
#   always allowed to make changes to this basic replication peer
#   replica backend.  The presence of this option indicates that this
#   LDAP server is a peer replica for this backend, and can receive
#   updates from another peer LDAP server using the specified DN as
#   well as processing updates received from clients.  The specified DN
#   is a special entry that should only be used when replicating to
#   this peer replica backend.  The DN has unrestricted update,
#   compare, and search access for all entries in the backend on this
#   server, even if the LDAP server is in maintenance mode.  When in
#   maintenance mode, only this DN and the LDAP administrator can
#   access and update the entries in this backend.
#
# Example:
#   peerServerDN "cn=Peer Server, o=Your Company"
#
# Notes:
#   You cannot specify the masterServerDN option in this section of the
#   configuration file.
#
#   The peerServerDN option indicates basic peer-to-peer replication is
#   configured for this backend section.  Therefore, the peerServerDN
#   option cannot be specified if the useAdvancedReplication option is
#   set to 'on' in the CDBM backend database section.
#----------------------------------------------------------------------
#peerServerDN "cn=Peer Server, o=Your Company"

#----------------------------------------------------------------------
# peerServerPW <password>
#
# Description:
#   The peerServerPW option specifies the password for the
#   peerServerDN that is allowed to make updates for this backend.
#   This option is only applicable for a basic replication peer
#   replica LDAP server.
#
# Example:
#   peerServerPW pPassword
#
# Notes:
#   Use of the peerServerPW configuration option is strongly
#   discouraged in production environments.  Instead, specify your
#   peerServerDN as the distinguished name of an existing entry in the
#   directory information tree, including a userPassword attribute.
#   This will eliminate passwords from the configuration file.
#
#   The peerServerPW option indicates basic peer-to-peer replication
#   is configured for this backend section.  Therefore, the
#   peerServerPW option cannot be specified if the
#   useAdvancedReplication option is set to 'on' in the CDBM backend
#   database section.
#----------------------------------------------------------------------
#peerServerPW secret

#----------------------------------------------------------------------
# persistentSearch <on | off>
#
# Default Value: off
#
# Description:
#   The persistentSearch option specifies whether this backend supports
#   the persistent search operation.
#
# Example:
#   persistentSearch on
#----------------------------------------------------------------------
#persistentSearch off

#----------------------------------------------------------------------
# pwCryptCompat <on | off>
#
# Default Value: on
#
# Description:
#   The pwCryptCompat option specifies whether to use an EBCDIC
#   version or a UTF-8 version of the crypt() algorithm, to encrypt
#   passwords when the crypt value is used for the pwEncryption option.
#   When set to on, the EBCDIC version of the crypt() algorithm is used
#   as the encryption method.  When set to off, the UTF-8 version is
#   used.
#
# Example:
#   pwCryptCompat off
#
# Notes:
#   ASCII is a subset of UTF-8.  When sharing LDAP directory data
#   between z/OS and an ASCII-based platform, specify off to ensure
#   that the encrypted value is the same on both platforms.
#----------------------------------------------------------------------
#pwCryptCompat on

#----------------------------------------------------------------------
# pwEncryption < none | crypt | MD5 | SHA | SSHA | SHA224 |
#                SSHA224 | SHA256 | SSHA256 | SHA384 | SSHA384 |
#                SHA512 | SSHA512 | DES:keylabel | AES:keylabel >
#
# Default Value: none
#
# Description:
#   The pwEncryption option specifies the encryption or hashing method
#   to use when storing the userPassword and ibm-slapdAdminPw attribute
#   values in this backend.
#
# Example:
#   pwEncryption crypt
#----------------------------------------------------------------------
#pwEncryption none

#----------------------------------------------------------------------
# readOnly <on | off>
#
# Default Value: off
#
# Description:
#   The readOnly option specifies whether or not this database can be
#   modified.
#
# Example:
#   readOnly on
#
# Notes:
#   For this backend, when running in multi-server mode, this option
#   should be the same for all of the LDAP servers in the cross-system
#   group since any LDAP server can potentially become the group owner.
#   Do not use this option in replica servers as this will not allow
#   replica updates, sent from the master server.
#----------------------------------------------------------------------
#readOnly off

#----------------------------------------------------------------------
# secretEncryption <none | DES:keylabel | AES:keylabel>
#
# Default Value: none
#
# Description:
#   The secretEncryption option specifies the encryption method to use
#   when storing the secretKey, replicaCredentials, ibm-replicaKeyPwd,
#   and ibm-slapdMasterPw attribute values in this backend.
#
# Example:
#   secretEncryption DES:deslabel
#
# Notes:
#   Applications may use the secretKey attribute type to store
#   sensitive data that needs to be encrypted in the directory and to
#   retrieve the data in clear text format.  This encryption method is
#   used to protect the replicaCredentials attribute values in this
#   backend when basic replication is enabled.  This encryption method
#   also protects the ibm-replicaKeyPwd and ibm-slapdMasterPw attribute
#   values in this backend when advanced replication is enabled.
#----------------------------------------------------------------------
#secretEncryption none

#----------------------------------------------------------------------
# sizeLimit <integer-number>
#
# Default Value: 500
#
# Description:
#   The sizeLimit option specifies the maximum number of entries in
#   this backend, to return for a search operation.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   sizeLimit 350
#----------------------------------------------------------------------
#sizeLimit 500

#----------------------------------------------------------------------
# timeLimit <integer-number>
#
# Default Value: 3600
#
# Description:
#   The timeLimit option specifies the maximum number of seconds this
#   backend will spend answering a search request.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   timeLimit 60
#
# Notes:
#   The default setting allows connections to stay active for 1 hour.
#----------------------------------------------------------------------
#timeLimit 3600

# ---------------------------------------------------------------------
# useAdvancedReplication <on | off>
#
# Default Value: off
#
# Description:
#   The useAdvancedReplication option specifies if the LDAP server
#   supports advanced replication.  If advanced replication is active,
#   then the masterServer, masterServerDN, masterServerPW, peerServer,
#   peerServerDN, and peerServerPW configuration options cannot be
#   specified in any LDBM, TDBM, or CDBM backends.
#
# Example:
#   useAdvancedReplication on
#
# Notes:
#   The LDAP server will not start when useAdvancedReplication on is
#   specified and entries with an objectclass of replicaObject are
#   present in a TDBM, LDBM, or CDBM backend.
#
#   The LDAP server will not start when useAdvancedReplication off is
#   specified and entries with an auxiliary objectclass of
#   ibm-replicationContext are present in a TDBM, LDBM, or CDBM
#   backend.
#
#   The server compatibility level must be at least 5 to use advanced
#   replication
# ---------------------------------------------------------------------
#useAdvancedReplication off

#----------------------------------------------------------------------
# useNativeAuth <selected | all | off>
#
# Default Value: off
#
# Description:
#   The useNativeAuth option specifies if the LDAP server will use
#   native authentication for this backend.
#
# Example:
#   useNativeAuth all
#
# Notes:
#   Use the following value when:
#     selected - only entries with the ibm-nativeId attribute that are
#                within subtrees (as specified by the
#                nativeAuthSubtree option) use native authentication
#     all      - all entries within native subtrees (as specified by
#                the nativeAuthSubtree option) use native
#                authentication; these entries can contain either the
#                ibm-nativeId or uid attribute to specify the RACF ID.
#                userid.
#     off      - no entries participate in native authentication
#----------------------------------------------------------------------
#useNativeAuth off


#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#
# File-based GDBM specific CONFIGURATION SETTINGS
#
# NOTE: The GDBM backend can be configured in one of two mutually
#       exclusive usages, either as a file-based change log backend
#       or as a DB2-based change log backend.  This file includes
#       a separate section for each use.
#
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------


#----------------------------------------------------------------------
# database dbtype dblibpath
#
# Description:
#   The database option marks the beginning of a new database section.
#
# Example:
#   database GDBM GLDBGD31/GLDBGD64
#
# Notes:
#   All global options must appear before the first database section.
#   An optional name may be specified to identify this backend.
#----------------------------------------------------------------------
#database GDBM GLDBGD31/GLDBGD64

#----------------------------------------------------------------------
# include <filename>
#
# Default Value:
#
# Description:
#   The include option will cause the specified file to be included
#   as part of the LDAP server configuration.
#
# Example:
#   include //'sys1.gdbm.parms'
#----------------------------------------------------------------------
#include "//'sys1.gdbm.parms'"

#----------------------------------------------------------------------
# changeLogging <on | off>
#
# Default Value: on
#
# Description:
#   The changeLogging option turns change logging on or off.  While
#   change logging is on, all change logging operations are allowed.
#   When change logging is off, change log entries can be searched,
#   modified and deleted, but no new change log entries can be created
#   and no automatic trimming of the change log is performed.
#
# Example:
#   changeLogging off
#----------------------------------------------------------------------
#changeLogging on

#----------------------------------------------------------------------
# changeLoggingParticipant <on | off>
#
# Default Value: on
#
# Description:
#   The changeLoggingParticipant option controls the logging of
#   modifications to the LDAP server schema entry.
#
# Example:
#   changeLoggingParticipant off
#
# Notes:
#   For this backend, this option does not turn on or off change
#   logging.  That is done by the changeLogging option.
#----------------------------------------------------------------------
#changeLoggingParticipant on

#----------------------------------------------------------------------
# changeLogMaxAge <integer>
#
# Default Value: 0
#
# Description:
#   The changeLogMaxAge option specifies the maximum age in seconds of
#   an entry in the change log.  Change log entries are deleted when
#   they have been in the change log longer that this value, except
#   if changeLogging off is specified.
#
# Example:
#   changeLogMaxAge 3600
#
# Notes:
#   A value of 0 indicates that there is no maximum age.
#----------------------------------------------------------------------
#changeLogMaxAge 0

#----------------------------------------------------------------------
# changeLogMaxEntries <integer>
#
# Default Value: 0
#
# Description:
#   The changeLogMaxEntries option specifies the maximum number of
#   entries that the change log can contain.  If the number of change
#   log entries exceeds this value and changeLogging on is specified,
#   change log entries with the lowest change numbers are deleted.
#
# Example:
#   changeLogMaxEntries 10000
#
# Notes:
#   A value of 0 indicates that there is no maximum number of entries
#   that the change log can contain.
#
#   For this backend, change log entries are deleted until the number
#   of remaining entries is the maximum.
#----------------------------------------------------------------------
#changeLogMaxEntries 0

#----------------------------------------------------------------------
# commitCheckpointEntries <integer>
#
# Default Value: 10000
#
# Description:
#   The commitCheckpointEntries option specifies the number of entries
#   in the checkpoint file.  An entry is added to the checkpoint file
#   each time a directory entry is added, changed, or deleted.
#   When the maximum number is reached, the entries in the checkpoint
#   file are merged into the database file and the entries are removed
#   from the checkpoint file.
#
# Example:
#   commitCheckPointEntries 5000
#
# Notes:
#   A value of 0 indicates there is no maximum.
#----------------------------------------------------------------------
#commitCheckpointEntries 10000

#----------------------------------------------------------------------
# commitCheckpointTOD <hh:mm>
#
# Default Value: 00:00
#
# Description:
#   The commitCheckpointTOD option specifies a time of day at which the
#   checkpoint file is merged into the database file.  An entry is
#   added to the checkpoint file each time a directory entry is added,
#   changed, or deleted.
#
# Example:
#   commitCheckPointTOD 10:00
#
# Notes:
#   The value must be between 00:00 and 23:59.  To disable time of day
#   checkpoint processing, specify a value outside of this range.
#----------------------------------------------------------------------
#commitCheckpointTOD 00:00

#----------------------------------------------------------------------
# databaseDirectory <name>
#
# Default Value: /var/ldap/gdbm
#
# Description:
#   The databaseDirectory option specifies the name of the file system
#   directory containing the data files used by this backend, to store
#   the directory data.
#
# Example:
#   databaseDirectory /home/myLdap/gdbmData
#
# Notes:
#   A fully-qualified directory path must be specified.  A unique file
#   system directory must be specified for each file-based backend.
#   In addition, when multi-server mode is active, the same directory
#   path must be specified for each instance of this backend within the
#   cross-system group.
#----------------------------------------------------------------------
#databaseDirectory /var/ldap/gdbm

#----------------------------------------------------------------------
# filterCacheBypassLimit <integer>
#
# Default Value: 100
#
# Description:
#   The filterCacheBypassLimit option specifies the maximum number of
#   returned entries allowed in the result set of any individual search
#   that will be stored in the search filter cache.  Search filters
#   that match more than this number of entries will not be added to
#   the search filter cache.  This option is useful for maintaining
#   the effectiveness of the search filter cache and the entry cache.
#   It can be used to prevent a few search requests with larger result
#   sets from dominating the contents of the entry cache.
#
# Example:
#   filterCacheBypassLimit 200
#
# Notes:
#   This value is not used when the filter cache is not in use.
#----------------------------------------------------------------------
#filterCacheBypassLimit 100

#----------------------------------------------------------------------
# filterCacheSize <integer>
#
# Default Value: 0
#
# Description:
#   The filterCacheSize option specifies the maximum number of filters
#   in the search filter cache.  This cache holds information related
#   to the mapping of search request to result sets.  Retrieval of
#   information from this cache avoids database read operations when
#   processing search requests.  Search requests which return more
#   entries than specified by the filterCacheBypassLimit configuration
#   option are not placed in the search filter cache.
#
# Example:
#   filterCacheSize 1000
#
# Notes:
#   A value of 0 indicates that the cache will not be used.
#----------------------------------------------------------------------
#filterCacheSize 0

#----------------------------------------------------------------------
# fileTerminate <terminate | recover>
#
# Default Value: recover
#
# Description:
#   The fileTerminate option specifies whether the LDAP server should
#   terminate when file system errors occur.  If terminate is specified
#   the LDAP server will terminate if any file system error is
#   detected.  Otherwise, if recover is specified, the LDAP server will
#   remain active, but this backend will go into read-only mode when a
#   file system error is detected.
#
# Example:
#   fileTerminate terminate
#----------------------------------------------------------------------
#fileTerminate recover

#----------------------------------------------------------------------
# multiserver <on | off>
#
# Default Value: off
#
# Description:
#   The multiserver option specifies the operating mode for this
#   backend.
#
# Example:
#   multiserver on
#
# Notes:
#   You can configure a backend to operate in single-server mode while
#   another backend operates in multi-server mode except when GDBM or
#   CDBM is configured.  When CDBM or GDBM is configured, all TDBM,
#   LDBM, GDBM, and CDBM backends must be configured to use the same
#   operating mode.
#----------------------------------------------------------------------
#multiserver off

#----------------------------------------------------------------------
# persistentSearch <on | off>
#
# Default Value: off
#
# Description:
#   The persistentSearch option specifies whether this backend supports
#   the persistent search operation.
#
# Example:
#   persistentSearch on
#----------------------------------------------------------------------
#persistentSearch off

#----------------------------------------------------------------------
# readOnly <on | off>
#
# Default Value: off
#
# Description:
#   The readOnly option specifies whether or not this database can be
#   modified.
#
# Example:
#   readOnly on
#
# NOTES:
#   For this backend, change log entries are not created and are not
#   trimmed (deleted) by the LDAP server when this option is set to on.
#----------------------------------------------------------------------
#readOnly off

#----------------------------------------------------------------------
# sizeLimit <integer-number>
#
# Default Value: 500
#
# Description:
#   The sizeLimit option specifies the maximum number of entries in
#   this backend, to return for a search operation.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   sizeLimit 350
#----------------------------------------------------------------------
#sizeLimit 500

#----------------------------------------------------------------------
# timeLimit <integer-number>
#
# Default Value: 3600
#
# Description:
#   The timeLimit option specifies the maximum number of seconds this
#   backend will spend answering a search request.  The number must be
#   between 0 and 2147483647.
#
# Example:
#   timeLimit 60
#
# Notes:
#   The default setting allows connections to stay active for 1 hour.
#----------------------------------------------------------------------
#timeLimit 3600

#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------

# EXOP-specific CONFIGURATION SETTINGS

#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------
#----------------------------------------------------------------------

#----------------------------------------------------------------------
# database dbtype dblibpath
#
# Description:
#   The database option marks the beginning of a new database section.
#
# Example:
#   database EXOP GLDXPD31/GLDXPD64
#
# Notes:
#   All global options must appear before the first database section.
#   An optional name may be specified to identify this backend.
#----------------------------------------------------------------------
#database EXOP GLDXPD31/GLDXPD64

#----------------------------------------------------------------------
# include <filename>
#
# Description:
#   The include option will cause the specified file to be included
#   as part of the LDAP server configuration.
#
# Example:
#   include //'sys1.exop.parms'
#----------------------------------------------------------------------
#include


# =====================================================================
#
# END OF CONFIGURATION FILE
#
# =====================================================================
